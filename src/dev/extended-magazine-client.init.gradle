allprojects {
    afterEvaluate {
        minecraft.runs.client {
            workingDirectory project.file('build/thermal-fluid-client')
            property 'afl.extendedMagazineProbe', 'true'
            args '--quickPlaySingleplayer', 'fluid_probe'
        }
    }
}
