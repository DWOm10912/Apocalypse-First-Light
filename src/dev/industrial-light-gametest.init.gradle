allprojects {
    afterEvaluate {
        def testDir = project.file("build/industrial-light-gametest-${System.currentTimeMillis()}")
        minecraft.runs.gameTestServer {
            workingDirectory testDir
            property 'forge.enabledGameTestNamespaces', 'afl_bridge_light_tests'
            property 'mixin.env.remapRefMap', 'true'
            property 'mixin.env.refMapRemappingFile', "${project.buildDir}/createSrgToMcp/output.srg"
        }
        tasks.matching { it.name == 'runGameTestServer' }.configureEach {
            doLast {
                def log = new File(testDir, 'logs/latest.log')
                if (!log.exists() || !log.getText('UTF-8').contains('[AFL BRIDGE TEST] PASS industrial light:')) {
                    throw new GradleException('Industrial-light regression did not execute; zero selected tests is not a pass')
                }
            }
        }
    }
}
