package com.antaurora.apofirstlight.worldgen.terrain;

/** Minimal observed surface distinction; ice is not a fluid classification. */
public enum SurfaceType {
    UNKNOWN,
    SOLID,
    ICE,
    FLUID_SURFACE
}
