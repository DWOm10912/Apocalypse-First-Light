package com.antaurora.apofirstlight.worldgen.spatial;

/** Spatial role, not a provider registration or asset category. */
public enum SpatialClaimType { PROTECTED_SITE, SITE, BUILDING, INFRASTRUCTURE, CONNECTION }
