### 1. Noise System V2

### 40. Service Pistol Reload第0帧转静态基线 — 当前源 / 待目视

- 新双手姿势已提升为静态anchor，编辑模式/Ready/Fire共同采用，Reload第0帧anchor轨道归零。
- Reload相对关键帧重算，完整动作1ms采样等价，Fire原轨道不变；没有重复叠加或重新设计。
- bbmodel/geo/animation已保存同步，build通过；Java/Display/scale不变，视觉仍待确认。
- 下方仅Reload迁移为历史；当前参数与验证边界见framework顶端。

- 用户10:11:27最新源的左右Reload reference动作已等价烘焙到真实hand anchors，原reference轨道清空。
- 保留用户0秒恒定动作，未补回位帧；Reload末端与Ready可能跳变，不能沿用旧回位PASS。
- geo/animation/FP Display已同步（Display仅保存精度），export --check与build通过，Java未改，未启动客户端。
- 参数与检查边界见framework顶端。以下Micro Polish为历史记录。

- fp_root静态X+3度；右anchor Y-.15/Z+.20；左手仅共同跟随，不改Display/Java/scale或枪几何。
- Reload只补偿fp_root X与right_hand_motion平移，.30–1.06s原主段数学等价，首尾连接新Ready。
- Fire原轨道、mag路径/-3.2侧移/.40/.95声音节点/1.30s不变。source/geo/animation已同步，build通过。
- 新Ready/Fire露臂与俯角视觉待验收；没有把数值等价当作视觉通过。下方为历史候选。

- 仅双手anchor：右手位置固定、rotation=(-74,12,8)；左手位移(+.25,-.25,+.20)、rotation=(-70,-8,-20)。
- 源与geo已同步；所有animation、gun、Display、arm scale与Java冻结。共享新朝向仍需Fire/Reload复验。
- build通过，右接触点/枪口/回位数值检查通过，视觉仍未确认；未启动新客户端。完整参数见framework顶端。
- 以下Final Grip为上一轮历史记录。

- 右anchor按掌/握把横向投影校准为X=-3.3064409003，Y/Z/rotation不变；允许Ready掌内重叠。
- Reload仅gun_model_root X-3.2平滑侧移与左motion接触跟随；初版+3.2被用户判定方向反了，已反转待复验。
  允许受控枪手分离；rotation/mag路径/节拍冻结。
- source/geo/animation同步，build通过；启动客户端实机验收中，V1视觉冻结=NO。
- 下方为上一候选历史；参数/验证边界以framework顶端为准。

- 本轮右anchor X -1.20、左anchor Y -.35；Reload fp_root +30度Z修正/X+.50，手腕X18度平滑轨道。
- 左手只补偿支撑位到操作位的平移；Fire/弹匣路径/音效/Java/Display不改。
- geo与animation已导出；数值与构建检查不等于视觉通过，未启动本轮客户端。
- 详细曲线见framework顶端。以下为上一轮用户源同步记录，不代表当前坐标。

- 使用用户09:17:40最新bbmodel；右anchor X=.50保留，未追加偏移。
- 左reference新摆位(-4,0,-1)/(-5,0,20)等价迁至left_hand_anchor，未恢复旧支撑位。
- 保留用户源gun X -3并同步完整geo；animation只新增源已有的gun零position轨道，其余轨道不变。
- processResources/build通过；Java/Display/音效不改。旧候选启动检查曾报gun pivot过期，
  最新资源已同步但Runtime一次性检查未重跑；客户端已退出且未自动重启，Ready目视验收待完成。
- 不把等价误差检查当作穿插修复或视觉通过；具体参数见framework顶端。

- ✅ 45 把筛选后的现实 TaCZ 枪械使用显式裸枪声音半径；原 M1911、UMP45、AK-47、Kar98、M107 五个锚点数值不变。
- ✅ 9 把排除枪械不写入显式表，继续使用现有枪械类别 / 通用 fallback；true suppressor 由 TaCZ `silence` 的 `use_silence_sound` 结构化布尔值识别，感染者半径至少降低 90% 且保留 8 格下限与 TaCZ 更强 modifier；非 Suppressor MUZZLE 不套 cap。
- ✅ 所有服务端 `ExplosionEvent.Detonate` 爆炸统一产生一次 `EXPLOSION` Noise Event，爆心为声源，半径为 `clamp(round((96 + strength × 24) / 8) × 8, 96, 256)`；原版 TNT strength 4 对应 192 格。
- ✅ Explosion Noise 与现有 Explosion Tinnitus 独立，不改伤害、击退、感染者 AI 或 TaCZ 资源。
- ✅ Gunshot Tinnitus V1 使用独立 `base + distance_addend` 声学输入；true suppressor 在 listener 查询前清零整次 Gunshot Exposure，射手与附近玩家均不累积，且不影响 Explosion Tinnitus。
- ✅ Gunshot Tinnitus Episode Fix：枪声与爆炸共用一个 Episode，首个 impulse 只启动一次 one-shot；后续只 strengthen / extend，不 stop/new/replay，也不重置 Overlay，Episode 自然结束后 re-arm。
- ✅ 2026-09-06 隔离 GameTest Server 40/40 通过：覆盖 45/9 裸枪表、M1911/AKM/M107 90% cap、AKM 非 Suppressor、8 格下限、TaCZ 更强 modifier、感染者距离响应、AKM/SCAR-H/M107 Suppressor exposure=0、裸枪累积、packet throttle、Episode 与 Explosion 回归；图形客户端听感仍待实机验收。

#### 1.1 感染者增援与破障 AI 性能

- ✅ Vanilla Zombie Reinforcement 保持双层禁用：加入世界 / 转换后清空 `minecraft:zombie.spawn_reinforcements` 的 base 与全部 modifier，任何 `SummonAidEvent` 再由 `LOWEST` handler 返回 `DENY`；精确普通 Zombie 同时关闭 Vanilla `BreakDoorGoal`，只保留 AFL 受控破障入口。
- ✅ 破障前方扫描改为每实体 `10–20 ticks` 错峰，失败后 `20–40 ticks`；活动破坏不再重复全扫描。入口候选保持 `40 ticks` 冷却与 10 格范围，只对最佳 8 个候选建路；移动路径最短 `20 ticks` 刷新，失败后等待 `30–40 ticks`。
- ✅ 噪声广播只写听觉状态；首次调查寻路错开 `0–9 ticks`。2 格内重复噪声只刷新最后事件时间，不重置调查阶段、搜索点或目标 revision。
- ✅ 同一方块最多 1 个 breaker claim，死亡、移除、停止、方块失效或 60 ticks 超时后释放；`#apocalypse_firstlight:infected_breakable` 内容、破坏速度与掉落逻辑未改变。
- ✅ 2026-09-06 无图形 GameTest Server 49/49 通过：覆盖 Hard + `doMobSpawning=false` 下 40 次受伤事件全部 `DENY` 且无增援、所有 Zombie 子类属性归零、重复噪声、失败寻路冷却、玻璃破坏回归、同块 claim，以及 20 / 40 / 80 个感染者的 100-tick 压测（139 / 257 / 544 次局部扫描）。
- ⏳ 图形客户端未启动；真实帧时间、破坏动作、视觉效果与大型存档中的体感仍待实机验证。

---
### 2. 城市规划和生成

City Area / 城市区域生成
道路网络
主干道 / 次干道 / 住宅道路

City Block / 街区
Lot / 建筑地块
房屋自动朝向道路
建筑尺寸分级（Small / Medium / Large）

区域划分：
住宅区
商业区
工业区
市中心

道路：
沥青 / 混凝土道路
人行道
路缘
车道线
停车场
住宅车道

草坪 / 绿化带程序化生成
白杨树 / 灌木 / 路灯等道路装饰

AFL 的聚落类型与当前群系解耦。任何战前聚落原则上都可以存在于任何战后环境；环境只影响植被、地表、腐朽程度和破坏程度。核打击与战略目标系统进一步决定局部破坏强度，而建筑材料决定最终能够残存多少结构。

-------------------
### 3. 地堡的设施更新

储物柜 / 货架随机刷出初始生活资源
收音机 / 无线电消息系统
基础教学 / 世界背景信息（教程书）
开局物资随机化

---


### 4. 搜刮系统

不同建筑类型拥有不同 Loot Pool

例如：
超市 -> 食物 / 饮料 / 日用品
住宅 -> 生活用品 / 少量食物 / 工具
仓库 -> 工业材料 / 工具
医院 -> 医疗用品
警局 -> 武器 / 弹药
加油站 -> 食物 / 燃料 / 杂物

容器随机 Loot
Retail Shelf 随机商品
Metal Locker 随机内容

空货架概率
稀有物品概率
建筑整体 Loot 密度控制

世界生成时生成 Loot
避免重复生成 / 重载刷物资

---

### 5. 辐射系统附加

辐射值 / 辐射剂量

辐射来源：
辐射群系
核爆区域
污染建筑
核废料
特殊矿石 / 材料

辐射效果
辐射病阶段
防辐射服
防毒面具 / 过滤装置
辐射检测设备（盖革计数器）

------

### 6. 替换贴图

需要替换：
- 铝土矿
- 方铅矿
- 闪锌矿
- 锡石矿
用图像生成

---

### 7. 放射性污染

- ✅ Contamination Core V1A：ItemStack 离散污染数据、Tooltip 与调试命令（用户实机验证通过）
- ✅ Block Drops Contamination V1B（用户实机验证通过）
- ✅ World Loot Contamination V1B（用户实机验证通过）
- ✅ Level → RU/h Source V1C（用户实机验证通过）
- ✅ Player Carried Radiation V1C（用户实机验证通过；`32×SEVERE = 10.24 RU/h`）
- ✅ Geiger Precise Item Tooltip V1C（Tooltip 与中文本地化用户实机验证通过；英文未单独验证）
- ✅ Container Radiation V1.1：普通容器 0% 屏蔽、32 格 world-local 点源（point shielding 1/2/3 层用户实机验证通过）
- ✅ Lead Chest V1：独立 27 槽、50% 自屏蔽、GeckoLib 模型与 0.5 秒开关动画（待实机）
- ⏳ Radiation Hazard Container（95% 自屏蔽候选，未冻结）
- ⏳ Environmental Contamination Accumulation

---

### 8. 流体管道

- ✅ `apocalypse_firstlight:fluid_pipe` Block 与唯一可获得的 BlockItem 已注册。
- ✅ 六方向连接、Sparse Core 视觉规则和直线内部模型已接入；Straight 复用 Arm 贴图。
- ✅ V1 使用静态透明管视觉。
- ✅ 流体管道会在 `fluid_tank` 上方或下方显示端口连接臂；储罐水平相邻时不连接。Chemical Reactor 左、右流体口可显示连接臂。
- ✅ V1.5 储罐端口不再替代管道起始 Core：紧邻储罐的管段使用现有 Core + Arm 接到固定接口表面，其余管道几何和拓扑规则保持原样。
- ✅ V1.3 实际传输为无重力工业压力输送：任意高度的合法 drain 源与 fill 目标可经已加载管网连通；共享传输请求和每个真实自动端口均限制为同一 tick 累计最多 `25 mB`，最短路径优先，最多遍历 512 个管道节点，不强制加载区块。
- ✅ Fluid Tank 保持底部 drain / 顶部 fill；Chemical Reactor 保持玩家视角左侧 fill-only 输入、右侧 drain-only 废液输出。各输入、输出端口预算彼此独立，SIMULATE 只读取剩余预算，EXECUTE 按实际成功量扣减。
- ✅ Fluid Tank 与 Chemical Reactor 废液槽都由各自服务器 tick 主动 push，并复用 capability 驱动的共享 BFS；向上、向下和同高路径都不再受 Y 坐标过滤。
- ✅ 管道保持无 Block Entity、无 `FluidTank`、无残留库存；传输只改变源/目标储罐中的真实 `FluidStack`。
- ✅ 活跃路线动态流体渲染已实现：同步实际路径、6-bit 方向 mask 与 `isFlowing`；V1.4.1 真实传输使用 Fluid 的 FLOWING sprite，零传输背压使用 STILL sprite，复用对应 tint 与 atlas 动画。模式变化原位同步，在 6×6 管壳内绘制 4×4 核心并保留 20 ticks。
- ✅ V1.4 满载背压视觉已实现：目标容量已满或本 tick 输入预算耗尽、但端口和流体仍兼容时，在没有可立即填充目标的前提下继续刷新同一 Source → Target 路线；本 tick 传输 `0 mB`，不产生管内库存。可立即填充目标优先，目标恢复接收后回到 `25 mB/t`，源空、断网或目标失效后停止刷新并在既有 20 ticks 保留期结束时清除。
- ✅ 直接紧贴的同轴平行管线不会侧向自动连接；视觉连接与 BFS 使用同一静态拓扑规则，跨接需至少留一格并放置横向桥接管，且规则与流体类型无关。
- ✅ 本轮服务器 GameTest 已验证 Reactor 四朝向右侧上行 / 水平管路的 25 mB/t、独立左右预算、40 ticks 转移 1,000 mB 及满目标暂停 / 恢复；多源多目标与六面手动交互的完整客户端回归仍待进行。用户报告的具体废液管路故障尚未复现，未宣称已修复。
- ⏳ 水/岩浆 FLOWING ↔ STILL 状态切换、转角连接、T 分支空白、端口衔接和透明壳 z-fighting 仍待客户端实机视觉检查。

---

### 9. 流体储罐

- ✅ `apocalypse_firstlight:fluid_tank` Block、BlockItem 与 Block Entity 已注册。
- ✅ 每格容量为 `20,000 mB`；直接竖直相邻时从下往上按最多 4 格分组，组合容量为 `20k / 40k / 60k / 80k`，第 5 格开始独立组。
- ✅ 每组由最底部 Block Entity 保存唯一权威 `FluidStack`，成员转发访问；自动化保持整组最顶部只输入、最底部只输出，内部接合面与水平侧面无流体能力，顶/底端口分别执行 `25 mB/t` 累计预算。
- ✅ 玩家手持正常 Forge 流体容器可从整组任意成员的六个面操作控制器统一储液槽，单次桶交互不受自动端口速率限制；控制器容量、流体 NBT 与内容变化客户端同步保持不变。
- ✅ V2 baked 外壳使用用户提供的自定义静态模型和 128×128 atlas；V1.5 外露顶/底 slab 固定使用原封闭端口 multipart cap，不再按 Pipe 连接状态切换透明中心。专用 connected 模型和透明贴图已删除。
- ✅ 直接堆叠的内部 cap/端口板被省略，以 join frame 保留连续角柱；只有真实组端点显示固定端口，堆叠内部视觉不受 V1.5 调整影响。
- ✅ BER 继续只渲染流体，使用 still sprite、tint 和控制器总储量；跨成员形成连续液柱且只保留一个真实液面，边界约为 `1.02..14.98`。
- ✅ 拓扑变化按下方组优先、再上方组重分配，容量不足的溢出在 V1 丢失；无流体复制。
- ✅ 服务器 GameTest 已验证 4+1 确定性分组、80k 端点与 NBT 重载、拆中层后的 `20k + 40k` 守恒分配。
- ✅ 生存模式正常挖掘会让掉落储罐保存被破坏成员按 bottom-up 计算的局部流体切片，单个物品最多 `20,000 mB`；世界剩余流体继续交给既有下方优先重分配。
- ✅ 储罐 BlockItem（包括空储罐）最大堆叠数为 1；带液储罐独立放置或加入空/同液组时恢复内容，加入实际同组的异液储罐时拒绝放置。
- ✅ 带液储罐物品 Tooltip 显示本格流体的本地化名称，以及带千分位的当前储量 / 固定 `20,000 mB` 单格容量；空储罐不额外显示空状态。
- ✅ 储罐物品使用 standalone Tank baked 外壳和物品 NBT 中的实际 Fluid still sprite 动态渲染本格液位；高度按固定 `20,000 mB` 单格容量计算，并复用世界储罐腔体边界。
- ✅ 爆炸掉落保持 clean Tank item；未添加生存配方、GUI、泵、压力或超过竖直 4 格的逻辑组。
- ⏳ 游戏内透明排序、V1.5 固定端口与 Pipe Core + Arm 交界、水/岩浆外观、连接组合、容器交互和重载持久化仍待实机验证；新增物品液位的 GUI、热键栏、手持与掉落物显示同样待实机确认。

---

### 10. 机器拆放内容保留

- ✅ Thermal Generator、Energy Cell、Crusher、Industrial Furnace、Compressor 与 Alloy Furnace 正常掉落保存当前 FE；Chemical Reactor 额外保存输入液槽与废液槽。重新放置时按当前容量 clamp 后恢复。
- ✅ 七种可带 FE 的机器 BlockItem 最大堆叠数均为 1，避免带内容物品堆叠造成复制。
- ✅ 七种机器的带电物品显示带千分位的 FE Tooltip；Reactor 额外显示 FE / capacity 和非空 Input / Waste 的真实流体名称、储量 / 8,000 mB，其余机器的旧 FE 格式不变；`0 FE` 继续隐藏。Tank 显示非空 Fluid / 20,000 mB。损坏 NBT 安全读取，不改变保存格式或写回 hover 数据。
- ✅ Reactor 与水 / 工业废液 Tank 的 Survival 钻石镐拆除 → Tooltip → BlockItem 重新放置已通过服务器 GameTest；图形客户端 hover 外观仍待检查。
- ✅ Energy Cell 默认充电模式（只接收），可通过 GUI 右上角按钮切换为放电模式（只输出并作为 Power Cable producer）；模式随世界保存及正常拆放保留。
- ✅ Power Network V2：Level tick 末尾按连通 Cable component 聚合 producer，fair-share 与 leftover 重分配，余数按 tick 轮转；最小 BlockPos key 加每 tick 去重，无线缆储能/吞吐/损耗。5 项服务器测试通过：1/3/6 Thermal 供电、4/30/30、模式和隔离网络、FE 守恒；客户端综合场景待验证。
- ✅ 机器库存、燃料、输出与加工进度不写入机器掉落物；仍按既有规则独立掉落或清空。

---

### 11. 采掘工具与工业回收等级

- 工业机器、工业管网、强化结构和高价值金属回收物统一要求 Diamond-tier Pickaxe；其中包括机器、Power Cable、Fluid Pipe、Fluid Tank、Lead Chest 与 Commercial Glass Double Door。
- 所有 AFL 工业矿石统一要求 Diamond-tier Pickaxe：Bauxite、Galena、Sphalerite、Cassiterite、Pentlandite、Spodumene 与 Wolframite。
- Retail Shelf Single 保持普通商业设施定位，要求 Iron-tier Pickaxe。
- Fallout/Scorched Soil 使用 Shovel；Fused Ground 与 Asphalt 使用 Stone-tier Pickaxe；道路标线、白杨木、树叶和树苗继续遵守材质对应的 Vanilla-style 规则。

---

### 12. 钨材料链

- ✅ 已注册氧化钨、钨粉、碳化钨粉与硬质合金锭四种基础材料物品，并接入 AFL Creative Tab、英中本地化和物品模型。
- ✅ 已完成 7 步生产线：矿石粉碎 1–3 黑钨矿、1000 mB 水湿法处理并产 1000 mB 工业废液、氧化钨还原、钨粉烧结、钨板压制、碳化钨粉与硬质合金合金冶炼。
- ✅ 新增 `chemical_reacting` 与 `industrial_smelting` recipe type/serializer；后者优先于 blasting/smelting 且直接使用 JSON ticks，氧化钨/钨粉没有 Vanilla 炉配方绕过。
- ✅ 五类机器配方均通过 RecipeManager 自动进入 JEI；Chemical 使用原生 fluid ingredient 槽，新分类具备 catalyst、layout 来源 click area、本地化加工时间与同步 balance 派生能耗。
- ✅ 2026-09-05 隔离 GameTest Server 总计 13/13 通过，其中 4 项钨回归覆盖配方数值、Coal/Charcoal、无 Vanilla 绕过、Chemical 原子结算及物品/废液堵塞暂停恢复，以及五台机器的精确时间/能耗与产出；JEI 与 GUI 图形观感仍待客户端实测。

---

### 13. 化学反应器

#### 关联流体：工业废液 V1

- ✅ 已注册 `industrial_waste` Source / FluidType / LiquidBlock、`flowing_industrial_waste` 与 `industrial_waste_bucket`；桶为 `1,000 mB`，加入 Fluid Tank 后的创造栏位置，具备英中本地化。
- ✅ 使用用户原始 still / flow PNG（各 8 帧，3 ticks/frame），白色 tint、translucent、自有 atlas sprite 与动态桶模型；当前实体交互声音使用 Vanilla Water SoundEvent，不保留自定义 OGG。
- ✅ 依据本地 WaterFluid / Forge WATER_TYPE 对齐公开流动参数、移动属性、灭火、船、补水与桶声音；明确关闭 source conversion，不加入 water / lava tag。
- ✅ Fluid Tank、Fluid Pipe 和 Chemical Reactor 保持既有通用 FluidStack / capability / 渲染实现，不改 `25 mB/t` 或机器逻辑。
- ✅ 黑钨矿湿法处理配方每次向反应器废液槽产出 `1,000 mB` 工业废液；局部工业 / 城市世界生成仍为后续任务，当前无污染、辐射或额外接触伤害。
- ✅ 2026-09-05 隔离 DEV GameTest Server 6/6 通过：单源流动及退去、`S _ S` / 2×2 不补源、连续取桶 Source 数量递减、生存模式完整桶取放、向下流动、Water 公开属性 / 声音事件对齐，以及桶灌储罐 1000 mB、管道 25 mB/t 上限与守恒、反应器废液存储。
- ⏳ 客户端 still/flow 动画、颜色、声音听感、实体移动 / 船 / 灭火体验及反应器液条观感未验证。参数、路径、测试复跑命令与水语义差异见 `docs/01 - 系统设计/工业.md`。

#### 工业废液实体音效 V1

- ✅ 自定义接触 / 移动触发层保留；small splash、high-speed splash、swim 改用实体自身的 Vanilla Water SoundEvent。玩家分别使用 `PLAYER_SPLASH`、`PLAYER_SPLASH_HIGH_SPEED`、`PLAYER_SWIM`，其它实体默认使用 `GENERIC_SPLASH` / `GENERIC_SWIM`。
- ✅ 0.25 大小分界和约 0.6–1.4 随机 pitch 与本地 1.20.1 `Entity` 公式一致；splash 播放音量在 Vanilla 结果上统一乘 `1.25` 并限制最大 `1.0`，分支仍按放大前数值判定。swim 音量不变，删除 small / sprinting 的人工 pitch 变体。停止移动不继续 swim，持续浸泡不重复 splash。
- ✅ FluidType 的桶装入 / 倒出继续直接使用 `BUCKET_FILL` / `BUCKET_EMPTY`；未添加 water tag，也未改 FluidType、禁补源、物理、水下雾、纹理或机器。
- ✅ 删除 AFL `industrial_waste_splash` / `industrial_waste_swim` 注册、sounds.json 项和正式资源 OGG；不复制 Vanilla OGG，用户下载目录中的原始素材不处理。
- ✅ 隔离 GameTest Server 8/8 通过：玩家 / Cow / ItemEntity 的 small / high-speed splash、swim 事件和 Vanilla 参数、单次派发、节奏、停止与 Silent 抑制，以及流体、禁补源、桶声音和雾判定回归。
- ⏳ `Entity.playSound` 已恢复 Vanilla 客户端 / 服务端 authority，代码路径避免同一玩家收到双份；实际客户端听感、多人双端无叠音及 Water 对比仍待实机验证。

#### 工业废液水下雾 V1

- ✅ 通过 Forge `ViewportEvent.ComputeFogColor` / `RenderFog` 的客户端 LOWEST 处理设置专属雾，不修改全局 FogRenderer 或原有辐射代码。
- ✅ 仅实际相机位置位于废液 Source / Flowing 的液面下生效；类型与液面高度双重检查，脚部入液、旁边、空气和其它流体不触发。
- ✅ 初始雾色 RGB `(70,71,54)` / `#464736`，依据 still PNG 平均色压暗；start=`1.5` 格、end=`10` 格、SPHERE，未改纹理、tint、声音、流体物理、禁补源或机器。
- ✅ 隔离 GameTest Server 8/8 通过：新增相机位置 / 液面边界判定用例，外加原有 6 项流体和 1 项音效回归；compileJava / processResources / build（offline）及 `git diff --check` 通过，DEV 类不打包。
- ⏳ 无界面液面判定测试不等于视觉验收；图形客户端尚未启动，进入 / 离开画面、颜色与视距微调、重复进出无闪烁、Jade / JEI / GUI 兼容仍待实测。
- ⏳ 尚无自定义 overlay、shader、后处理、粒子或污染 / 辐射特效。参数、实现路径与验证边界见 `docs/01 - 系统设计/工业.md`。

#### 机器本体

- ✅ `apocalypse_firstlight:chemical_reactor` 方块、BlockItem、Block Entity、Menu/Screen、水平朝向、静态六面视觉与 Diamond-tier 采掘规则已接入。
- ✅ 运行前面板动画资源已预置，但当前方块始终使用 idle 前面板。
- ✅ FE 容量 `60,000 FE`、最大接收 `128 FE/t`；背面通过 Power Cable 输入，正常掉落/重放保留 FE。
- ✅ 两个独立 `8,000 mB` 液槽已实现：玩家面对机器正面时，左侧输入口仅填充，右侧废液口仅抽取，其余面不暴露流体能力。
- ✅ GUI 使用共享 layout 坐标，显示玩家背包、物品输入/输出、Vanilla Furnace 进度箭头、能量条、输入液与废液条；液体使用实际 still atlas sprite、tint 与自底向上的比例裁切。
- ✅ FE、输入液、废液、物品与加工进度保存到方块实体 NBT；正常拆机时物品内容独立掉落，机器物品继续保存 FE、输入液与废液。
- ✅ AFL Fluid Tank 可经 Fluid Pipe 向任意高度的反应器左侧输入口输送真实流体；源底部输出端和反应器左侧输入端各自同 tick 累计最多 `25 mB`。
- ✅ Chemical Reactor 废液槽非空时，会从玩家视角右侧 drain-only 端口主动向管网 push，可输出到任意高度 Fluid Tank 的顶部 fill 端口；源废液输出端和目标顶部输入端各自同 tick 累计最多 `25 mB`。
- ✅ V1.1 修正了左右侧方向反转，并让管道 BFS 通过目标相邻面的真实 `FLUID_HANDLER` 与模拟 `fill` 识别可填充终点；目标查询面使用“管道指向目标方向”的反方向。
- ✅ `32 FE/t` 正式用于 `chemical_reacting`：条件齐全才扣电推进，断电/缺水/废液满/输出堵塞时暂停，完成时原子消耗与产出，不受外部 `25 mB/t` 端口预算限制。
- ✅ Chemical JEI category、机器 catalyst、箭头 click area、原生流体槽与服务器 balance 同步能耗已接入；Jade 现已接入 Energy、非空输入液、真实 Processing、非空废液和非空 Output。数据与暂停进度已通过服务器测试，STILL/tint/动画/层级仍待客户端视觉验证。倾倒按钮、运行声音和粒子仍未实现。
- ⏳ GUI、能源线缆输入、无重力管道输入/废液输出、双槽 NBT 与拆放保留尚未进行客户端实机验证。

---

### 14. 合金炉固定工作功率

- ✅ 合金炉固定工作功率由机器 balance 统一定义为 `24 FE/t`；配方只保存输入、输出与加工时间。
- ✅ 钢锭配方保持 `200 ticks`，总能耗由 `24 × 200` 派生为 `4,800 FE`；JEI 使用同步后的机器工作功率计算显示。

---

### 15. 热力发电机燃料总发电量重平衡

- ✅ 正式燃料表：煤炭 `500 FE`、木炭 `500 FE`、煤炭块 `5,000 FE`、熔岩桶 `20,000 FE`；白名单仍仅这四项，熔岩桶仍返回空桶。
- ✅ 容量保持 `100,000 FE`，生成与最大输出保持 `16 FE/t`，满电暂停并在释放空间后继续同一份 fuel remaining。
- ✅ 每 tick 使用 `min(16, fuelEnergyRemaining, freeCapacity)`；煤炭 / 木炭最后一 tick 精确生成 `4 FE`，煤炭块最后一 tick生成 `8 FE`，不丢失或超发。
- ✅ JSON 是 datapack authority；Java safe fallback 同步四项新值。登录与 `/reload` 后继续通过既有 S2C fuel map 更新客户端燃料识别 / tooltip；正在燃烧的 remaining / total 维持原批次，不因 reload 重置或重复消费物品。
- ✅ 隔离 GameTest Server 与工业废液回归合计 9/9 通过：四种燃料精确总量、煤/木炭末 tick 4 FE、煤炭块末 tick 8 FE、熔岩桶容器、16 FE/t 生成 / 输出、满电暂停与同一 remaining 恢复均通过。
- ⏳ GUI / Jade 不显示燃料总 FE，未改布局或 tooltip 结构；客户端 tooltip 的 S2C 新值与 `/reload` 实机流程仍待图形客户端验证。

---

### 16. 爆炸耳鸣 / Explosion Tinnitus V1

- ✅ 服务端 `ExplosionEvent.Detonate` 保持原实际 radius 与眼睛距离 authority：`R=radius×3`、`n=clamp(1−d/R,0,1)`。音频仍为 `s=n²`、阈值 `0.15`；视觉已独立为线性 `v=n`、阈值 `0.08`。不依赖掉血、不随机、不改伤害/击退，不做墙体声学遮挡。
- ✅ 保留用户原始 OGG 和 `256×256 RGBA` PNG。音量仍为 `0.15+0.55s`、时长仍为 `round(40+120s)` ticks；Overlay 峰值改为 `0.18+0.42v`（最大 0.60），保持原 10% hold / 85% 归零的半余弦包络。视觉单独时间轴，范围边缘无声冲击不启动或延长耳鸣。
- ✅ 每客户端最多一条耳鸣实例；更强爆炸提升 severity 并延长同一 Episode，不再停止或重播 one-shot；较弱爆炸仅延长 20 ticks。单次 Episode 时间轴上限 200 ticks，结束后 re-arm。断线/卸载/死亡/重生清理；容器、菜单打开时不画 Overlay。
- ✅ 复用 AFL S2C 网络（当前协议 10；V0.4 增加原生手枪 C2S，耳鸣 S2C 未改），client-only sound/render 有 Dist 边界；没有世界声音压低、玩家 Sound Settings 修改、Potion Effect、图标、shader、疾病或永久听损系统。
- ✅ 本轮视觉修正复验：152,903 项时间轴断言通过，修改前后相同 100,000 tick 序列的音频状态指纹一致；隔离服务器再次 29/29 GameTest 通过。offline compileJava / processResources / build 与 diff 检查通过，JAR 内 PNG/OGG 未改且不含 DEV 类/WAV/preview。完整公式、对照表与验证边界见 `docs/01 - 系统设计/生存状态.md`。
- ✅ 用户已反馈初版音频、距离声音手感、Overlay 渲染和连续爆炸不叠音正常。
- ⏳ 本轮线性视觉修正尚待 1、3–4、5 格、视觉边缘和范围外截图；修改后 GUI/Jade、清理与听感未重新实机验证，不把数值 alpha 当作可见性证明。

---

### 17. 枪声耳鸣 / Gunshot Tinnitus V1

- ✅ TaCZ 每次成功服务端 `GunShootEvent` 仍只产生一次感染者 `GUNSHOT` Noise Event；感染者使用 true suppressor 的 90% cap，玩家 Exposure 使用独立的 `base + distance_addend` 声学输入。
- ✅ listener 仅来自同一 `ServerLevel` 的 16 格 AABB 玩家查询；射手按 0 格，附近玩家按眼睛到声源距离，Creative 包含，Spectator / dead / removed 排除，16 格及以外增加量为 0。
- ✅ 实际公式为 `loudness=clamp((R-64)/96,0,1)`、`distanceFactor=clamp(1-d/16,0,1)^2`、`shotExposure=clamp(loudness×distanceFactor×0.55×boost,0,0.75)`；`R>=128` 乘 `1.25`，`R>=160` 再乘 `1.35`。
- ✅ True suppressor 复用 TaCZ `silence` 的 `use_silence_sound` 布尔值判定，在 listener pass 前令整次 Gunshot Exposure 为 0；AKM、SCAR-H、M107 的射手与附近玩家均免疫本次枪声耳鸣，非 Suppressor MUZZLE 继续正常累积。
- ✅ Exposure 上限 1.5，按 `0.008/tick` 衰减，阈值 `0.35 / 0.60 / 0.90`；发送后保留 45%。Packet 保留 6 ticks 最短间隔，后续仅在 severity 提升至少 0.15 或每 20 ticks 需要延长时发送。
- ✅ 复用现有 packet、`ExplosionTinnitusClientState`、OGG 和 Overlay；Gunshot + Explosion 共用一个 Episode，只有 Episode 首包返回 `START` 并创建声音，更强包为 `STRENGTHEN`、其余为 `EXTEND`，均不 replay。结束后重新允许下一次 `START`；Explosion tuning 未改。
- ✅ 隔离 GameTest Server 40/40 通过，包含 AKM/SCAR-H/M107 true suppressor exposure=0、AKM 非 Suppressor、AKM/SCAR-H/Deagle/M107 裸枪累积、6/20 tick packet throttle、单 Episode 单 START、re-arm、M1911/AKM/M107 感染者 90% cap 与 TNT/Explosion 回归。
- ⏳ 未启动图形客户端；真实 TaCZ 开火听感、M107 附近队友画面、枪声与爆炸连续组合时的 OpenAL/Overlay 体验仍待实机验证。

---

### 18. 负重系统

### 19. Native AFL Gun Framework V0.4

- ✅ 已实现 `apocalypse_firstlight:service_pistol`（制式手枪），加入 AFL Items 创造标签；GeckoLib 4.7.4，沿用既有依赖。
- ✅ V0.3.8 完整资产导出：77 cube、16 bone、132 个精确时间关键帧、原 128×128 贴图；两个 reference arms 不导出，手部 anchors 保留。显示参数同步自源文件。
- ✅ 已实现左键半自动 Fire 请求、R Reload 请求、服务端主手/槽位验证、3 tick fire 间隔及 26 tick reload 锁；只注册并使用普通枪声与弹匣 out/in 三个声音。
- ✅ 无限弹药动画/音效原型；无弹药物品/弹量、伤害、hitscan、projectile、dry-fire、抛壳、双手 skin renderer、Noise、Tinnitus、Suppressor。TaCZ 依赖与资源保持不变。
- ✅ 清理 11 个旧模型与 1 个旧 PNG，保留 V0.3.8 和 rig template；可从 `.gradle-user/asset-backups/service-pistol-pre-v038-20260906.zip` 恢复。
- ✅ 修复动画取消包空名称导致的崩溃；修复版客户端实际资源/动画缓存与取消包编码自检 PASS，最终 offline build 通过，正式 JAR 不含 DEV 类或 TaCZ 复制资源。
- ✅ 用户在修复版复测后确认“正常”：开火/换弹中切槽或丢枪、GUI、第一/第三人称、枪声/换弹声与按键锁。V0.3.8 源文件哈希保持不变，未截图。
- ⏳ 创造标签取物、按住左键不连射、原版近战/挖掘抑制、地面显示、多人可见性与 TaCZ 回归尚未单独确认；不把编译或本轮笼统反馈扩大为这些项目的验收。详见 `docs/dev/native-afl-gun-framework-v0.md`。

### 20. Native AFL Gun Framework V0.4.1 — Runtime Presentation

- ✅ 已实现原创 32×32 静态图标与 GUI/3D 模型分流；非 GUI 继续使用正式 GeckoLib 资产。
- ✅ 已实现基于 animated hand anchors 的玩家双臂/袖层，复用玩家皮肤和原版 Classic/Slim 几何；reference arms 继续不导出。
- ✅ 已实现物品限定的第三人称双手 ArmPose，以及使用当前主手实例的第一人称独立渲染入口；没有全局 renderer 重写或其它物品姿势修改。
- ✅ 用户新增源文件编辑已通过原生导出检查：本次只变更 GUI display，geo/animation 与运行时一致；源文件由用户保留，Codex 未改。
- ⏳ 此版第一人称握持与换弹可读性未通过；后续由 V0.4.2 接替。第三人称与静态图标冻结，不用旧版笼统反馈代替新表现层验收。

### 21. Native AFL Gun Framework V0.4.2 — First-Person Grip / Reload

- ✅ 已校准右手掌内主握、左前下支撑与非对称前臂朝向；只改 runtime mapping，保留 source anchors、reference arms 和原版 Classic/Slim 几何。
- ✅ Ready camera-relative translation 为 `(0.60,-0.54,-0.64)`，原 display JSON 和 0.3 scale 不变；通过朝向与构图让前臂自然出屏，不裁短几何。
- ✅ 根因定位：实际 Reload controller、weapon_root 和左手轨道完整运行，主要弹匣运动落在旧取景下边界之外，并非静态 anchor 或动画被覆盖。
- ✅ 增加第一人称共用父变换：峰值位移 `(+0.05,+0.28,-0.25)`，Z/Y/X `-16/+32/+8°`；0–0.24s 平滑进入，保持至 0.85s，1.18s 归零。原 1.30s 动画及弹匣 handoff 未改。用户指出首版枪口朝右，已仅反转 yaw 至 +32°，方向修正版待复验。
- ✅ compileJava / processResources / build（offline）通过；音效文件、0.40/0.95s 时间点、第三人称与静态图标冻结。无 ADS、弹药系统或伤害扩展。
- ✅ 用户后续 V0.4.3 任务明确当前枪械 Ready/Fire/Reload 构图基本可接受并冻结；+32° yaw 后的旧弹匣出屏数值检查仍作为已知限制记录，不假称通过，也不在手臂任务中调整枪械。

### 22. Native AFL Gun Framework V0.4.3 — Player Arm Scale / Clipping

- ✅ 根因确认：手臂采用正确原版几何，但误继承 gun display 0.3 scale。现先计算完整 animated contact，再仅在手臂分支去除 basis 缩放；位置保留，不硬乘 3.33、不改枪械矩阵。
- ✅ Classic 4×12×4、Slim 3×12×4 复用原版 PlayerModel；保留玩家皮肤与左右 sleeve 开关、0.25px 外层，完整渲染六面，不裁短手臂。
- ✅ 保持原掌心接触点，修正手臂局部延伸方向为以下/外侧为主、略远离相机；不修改全局 near plane、depth、玩家整体 scale 或第三人称。
- ✅ Ready、Fire、Reload 曲线、源文件、枪模、图标、音效和玩法冻结；未截图、未 commit/push。
- ✅ offline compileJava / processResources / build、diff-check 通过；14 项冻结文件 SHA-256 不变，JAR 无 DEV 类或 TaCZ 资产。图形客户端 15:44:45–46 自检通过 Classic/Slim 完整六面与 sleeve、单位缩放/接触点、301 Reload + 41 Fire 样本近裁剪面检查。
- ✅ 旧版采样同样未触及 near plane，右臂短小主要符合 0.3 缩放与朝相机方向的透视缩短，不是缺 geometry；未修改全局投影。旧版弹匣投影 -1.0929165 限制保留并明确警告，不修改用户冻结的枪械表现。
- ⏳ 手臂粗细/完整观感、俯视、皮肤袖子、握持及弹匣无遮挡仍待用户实机验收；Classic 数值几何验证不等于 Classic skin 视觉通过。
- ✅ 实际入世界路径记录当前 Slim/efe 玩家皮肤、双侧 sleeve 开启、每手 base/sleeve 各 24 顶点和单位缩放；首批每手 85 样本未触及近裁剪面。视觉验收仍独立待用户反馈。
- ❌ 用户图片验收未通过：手部过大、Ready 穿套筒、Reload 遮挡握把/弹匣。单位缩放及近裁剪检查通过不代表握持体积合理；当前 V0.4.3 不标完成。需校准掌面接触，并确认是否允许独立第一人称手臂视觉缩放，枪械构图/动画继续冻结。
- ✅ 用户已明确允许独立 Arm Visual Scale。修正版设为 0.55，保留原版 Classic/Slim 几何与袖层；右/左掌心 Y 改为 9.65/9.85，接触 X 移向内侧 20% 臂宽，微调 anchor-local contact offset，不改源骨骼。
- ✅ 0.55 修正版 offline compileJava / processResources / build 通过，图形客户端 15:58:51 自检通过独立缩放、标准几何、接触点与近裁剪面；301 Reload + 41 Fire 样本的完整 sleeve 与套筒/枪管/瞄具 OBB 无交叉。14 个冻结文件哈希不变。
- ❌ 用户后续反馈 0.55 版 Ready 再现抱枪感、枪显大/贴脸；Reload 侧开可接受。Ready 恢复工作转入 V0.4.4，不把数值穿模检查当作整体视觉通过。

### 23. Native AFL Gun Framework V0.4.4 — Presentation Baseline Restore

- ✅ 通过历史文档、审计哈希和当前源码定位 V0.4.2；该模块仍未提交，不存在可宣称的 V0.4.2 commit。修改前 14 项冻结文件与 V0.4.2 留存快照一致。
- ✅ 未发现 gun scale / camera Z / root 数值改变，也未发现 push/pop 失衡或 arm 写回 weapon_root。枪显大归类 OTHER：已确认变化的是手臂构图，不能伪称枪矩阵被放大。
- ✅ 保持 V0.4.2 READY/FIRE gun matrix 和 Fire 源动画，恢复其略小手臂映射：独立 scale=0.30、原 offsets/palm Y/方向；不继续手臂尺寸试错。
- ✅ 保留当前 Reload +32° yaw、侧开位移、完整曲线与动画轨道。Weapon 与 Arms 各自复制独立 PoseStack/normal，push/finally/pop，手臂不写共享枪械矩阵。
- ✅ 高频 DEV 日志默认关闭，仅显式 debug flag 可开；未改源模型、贴图、音效、第三人称、图标、输入或玩法，未截图、未提交。
- ✅ offline compileJava / processResources / build 通过，图形客户端已启动；重复动作检查需包含 GeckoLib 原有 5 tick 自然回正期，不修改运行时动画参数来通过测试。
- ⚠️ 恢复的 V0.4.2 Classic 右手参考映射存在局部袖层/枪体交叉：按旧映射逐帧比较，不宣称本轮消除全部穿模。既有弹匣部分出屏限制也保留。
- ✅ 16:24:40 最终客户端数值自检通过：同一 controller 连续 40 动作 / 7,320 样本，两手 Classic/Slim 映射一致、矩阵隔离、自然回正后无累计漂移；301 Reload + 41 Fire 采样与旧基线交叉数量一致。枪械/音效冻结资源哈希保持。
- ✅ 用户对最终客户端分组验收回复「ok」：READY 大小/距离/主辅手关系可接受、FIRE 正常、当前 Reload 侧开保留、连续操作后回到同一位置。V0.4.4 表现基线验收通过；不扩展为全部皮肤/FOV 或零穿模验收。进一步 Arm Calibration 仍延期，下方用户追加的后续任务内容保留未改。

### 24. Native AFL Gun Framework V0.4.5 — Reload Right-Hand Grip

- ✅ 审计：旧 Reload 分支仍绘制完整 12 格右臂和袖层，侧开暴露没有连接身体的方柱末端；右 anchor 本身没有独立 Reload 轨道，不是再次位移或 root 泄漏。
- ✅ Reload 右手改为玩家真实 skin 的短掌段：Classic 4×4×4 / Slim 3×4×4，取末端手部 UV；可选外层同样截取手部，不压缩整条袖子贴图。沿用原握持矩阵和 0.30 scale，无独立右手换弹运动。
- ✅ READY/FIRE、完整左手路径、枪械侧开、弹匣与左手动画、音效、图标、第三人称、源模型全部冻结；结束或取消 Reload 自动返回原右臂路径，无持久短段状态。
- ❌ 构建通过，但 00:53:46 客户端自检发现短段与上部枪体交叉；用户另反馈右手几乎不可见。此厚短段方案未验收，已转 V0.4.5.1，不继续收缩厚方块。

### 25. Native AFL Gun Framework V0.4.5.1 — Reload Right Grip Surface

> 历史失败方案：侧面版也被用户否决并终止。下列数值通过不等于视觉通过；V0.4.7 已停用默认薄掌面路线，旧代码仅保留对照。

- ✅ 原厚段消失原因 DEPTH_OCCLUSION：当前侧开下位于枪体遮挡侧，旧几何射线 0/27 点可见。
- ❌ 首版薄掌面虽通过数值检查，用户实机反馈仍像卡进枪里：贴在握把背缘，只露出侧边窄条，属于 WRONG_FACE / WRONG_OFFSET，不算验收通过。
- ✅ 侧面修正版只改 Reload 右薄掌面：Classic 3.40×3.00×0.55 / Slim 2.55×3.00×0.55，贴在朝镜头的握把 -X 侧面；内侧中心 (-2.44,7.0,7.7)、法线 -X、epsilon 0.04 model units（非世界方块）；固定 anchor-relative mapping，正常深度测试。
- ✅ 只取玩家基础 skin 的右手末端侧面 UV，不再绘制任何 Reload 右袖层或完整前臂。Ready/Fire、左手、枪械侧开、弹匣/动画/音效/第三人称/图标均不改。
- 历史首版：offline build（37s）与 01:14:48–49 客户端数值检查通过，但视觉失败；不将这些结果沿用为侧面修正版验收。
- ✅ 侧面修正版 offline compileJava / processResources / build（41s）、git diff --check 与 77 cube / 16 bone / 132 key 资源检查通过。
- ✅ 01:24:47–48 新客户端数值复测通过：8 次 Reload / 2,094 有效采样，整个 frame 的 28 个 cube（含握把外壳/装饰板/web）、上部枪体、扳机和弹匣无几何交叉；固定相对握把、恢复 Ready、Classic/Slim UV 与射线检查通过；原 40 动作 / 7,320 样本矩阵回归仍通过。此为数值结果。
- ❌ 侧面修正版用户实机未通过，已终止；不继续微调薄掌面。未截图、未生成 preview、未 commit/push。

### 26. Native AFL Gun Framework V0.4.7 — Canonical Hand Locator

- 已实现标准 frame：手端截面中心为 origin，forearm=-Y、palm normal=+Z、lateral=+X；固定 Vanilla→canonical 平移绑定，binding scale=1、继承现有 display 0.30。
- Ready/Fire/Reload 统一完整 Classic/Slim arm+sleeve；使用玩家 skin，保留袖层开关/隐身/正常深度，不再默认使用短手、薄掌面或武器专用方向 quaternion。
- 源参考臂保持 4×12×4/export=false，原参考变换折算到 canonical locator；旧 anchor 作为 motion carrier 保留原左手轨道。18 runtime bones、77 原枪 cube、132 原关键帧；只同步 locator 与动画目标名，不改枪、取景、音效、弹匣或玩法。
- 新规范：`docs/dev/native-gun-hand-locator-authoring-standard.md`。DEV 数值等价 gate → 单右手 READY → 双手 READY → FIRE → RELOAD → 重复/皮肤，必须逐步验收。
- 当前 IMPLEMENTED ≠ VISUALLY ACCEPTED；全部用户视觉 gate 尚未通过，旧 ReloadGrip 暂不删除、也不回退为默认路径。构建/数值结果见 framework 文档。
- ✅ Offline compileJava/processResources/build、资源 verifier、diff-check 通过；02:11:09 图形启动数值 gate 通过：Classic 3.424e-7、Slim 3.339e-7，允差 1e-5；40 动作/7,320 帧，右手 grip-relative drift 7.451e-7。
- ⏳ 当前停在 B1 单右手 READY 用户验收。左手暂为 DEV-only 隐藏；双手/FIRE/RELOAD 用户视觉均 NOT_TESTED，不沿用旧版 sign-off。没有截图或 preview。

### 27. Native AFL Gun Framework V0.4.7.1 — Third-Person ArmPose Crash Fix

- 已定位 02:14:19 世界玩家渲染崩溃：custom ArmPose ordinal=10，已初始化的 Vanilla switch map length=10；不是第一人称 binding 错误。
- 用户同意最小替代：Service Pistol 主手返回合法原版 CROSSBOW_HOLD（ordinal=6），不再创建扩展 enum；由 Vanilla 应用主/辅双臂及惯用手镜像。不新增 post-setupAnim hook，不保留旧自定义角度逐值一致。
- V0.4.7 first-person locator/B_skin/参考臂/资产/取景/音效与 B1 单右手状态冻结；未新增 mixin、renderer 或 switch-table hack。
- DEV 检查覆盖 Classic/Slim、左右惯用手、蹲姿、手枪/木棍/空手转换；实际世界/F5/切物品/丢弃拾取及视觉验收待本轮复测。构建与结果记录于 framework 文档。
- ✅ Offline compileJava（37s）/processResources（25s）/build（28s）、资产 verifier、diff-check 通过；哈希确认本轮已有 src 文件仅 Third-Person PlayerPose 改动，另新增一个 DEV 检查类。第一人称及 B1 文件完全未变。
- ✅ 02:23:20 进入世界后 960 次 setupAnim 数值检查通过；02:23:26 日志确认 Slim/RIGHT 玩家持枪完成 F5 背面与正面 PlayerRenderer 渲染，原越界未复现。重复切换、实际丢弃拾取和姿势视觉仍待用户确认；不等于 B1 通过。



### 28. Native AFL Gun Framework V0.4.8 — First-Person Proportion Calibration

- 仅调整 Ready/Fire/Reload 共同 camera parent：右惯用手 T=(0.55,-0.47,-0.66)、yaw=-4°、pitch=-4°、uniform scale=0.92；左惯用手镜像 X/yaw。display=0.30 不改，共同有效 scale=0.276。
- Canonical Hand Locator、B_skin=1、两个 anchor 与完整 arm/sleeve 路线不变；不单独放大右手，不加状态特例。通过投影/距离/前臂露出校准观感，不改变枪手物理尺寸比。
- Reload 时间线/侧开曲线/弹匣/左手逻辑、枪模贴图、第三人称、HUD、音效均冻结；DEV 左手仍隐藏供单右手验收。
- ✅ Offline compileJava（36s）/processResources（25s）/build（27s）、资源 verifier、diff-check 通过；1,086 个 src 文件哈希对比仅共同 Presentation/FirstPerson 与 DEV 契约检查三个文件变化，正式 JAR 不含 DEV 类。
- ✅ 02:37:27 图形客户端启动；02:37:51 数值 PASS：Classic 4.3142657e-7、Slim 3.8346974e-7、右手相对握把漂移 8.940697e-7，均低于 1e-5；40 动作/7,320 帧。
- ⏳ 用户先验单右手 READY，之后 Fire/Reload，所有新比例视觉结果 PENDING_USER。数值通过不等于接触/遮挡/自然度通过。无截图、preview、commit/push。

### 29. Native AFL Gun Framework V0.4.8.1 — READY Grip + Proportion Rebuild

- V0.4.8 用户截图未通过：右手侧贴/横伸、手细枪大；本轮只做单右手 READY。
- 右 locator source origin=(0,6.349441,12.307031)、rotation=(-10,0,0)，原 carrier 不改；最终手端=(0,8.8,12.9)，前臂=(0,-0.848048,0.529919)，位于握把后上中部、向下后延伸。
- 固定 B_skin=S(1.4)×T(-centreX,-10,0)/16，完整原版 geometry/UV/sleeve 不变；共同取景 T=(0.51,-0.44,-0.70)、yaw/pitch=-4°、scale=.82，枪有效 .246、手 .3444。无按状态缩放/短手/薄掌面。
- 右 source reference 与 runtime anchor 同步；77 枪体 cube、132 关键帧、左 locator/轨道和 DEV 隐藏不变。通用 1.4 会同时影响未来启用的左手显示，不代表恢复左手方案。
- 第三人称、音效、图标、HUD、弹匣逻辑不动；Fire/Reload 只继承共享基线，不做本轮视觉调优。
- ✅ Offline compileJava（36s）/processResources（26s）/build（26s）、资产检查及 diff-check 通过；02:51:52 客户端启动，02:52:14 数值通过（40 动作/7,320 帧，Classic/Slim 等价与 grip drift 均小于 1e-5）。
- ⏳ READY 视觉 NOT PASSED / PENDING_USER，等待新版截图。握把重合是有意的，不声称完全无交叉；数值预览显式施加 1.4，不声称原生 BB 自动匹配最终显示。不生成截图/preview，不 commit/push。

### 30. Native Service Pistol — Weapon-to-Hand Physical Recalibration

- 前一版 V0.4.8.1 用户截图明确 FAIL；不保留其 1.4 放大手臂作为当前方案。
- gun 子树 77 cube/12 bone 绕 (0,8,6) 资产级 .5；握把宽 4.8→2.4。手臂标准尺寸/B_skin=1，camera 文件不改。右 hand locator 独立重放，不缩整套 weapon_root。
- 65 个枪内 position keys 减半；slide 峰值 3.2→1.6，magazine/reload_magazine 同步；muzzle/ejection/sight pivot 同步。weapon_root 为共享持握运动，原路径保留。
- 左 locator/reference 不改；20 个已有 left_hand_motion keys 仅按缩后的弹匣工作空间同步，端点归零，左手仍隐藏。所有 rotation/scale/时间/音效不变。
- 76 box UV 固定成逐面原取样，贴图未重画。非第一人称 gun 子树 F^-1 补偿以保留原显示，第三人称 pose/静态图标/HUD/玩法不动。
- ✅ Offline compileJava（34s）/processResources（24s）/build（26s）、资产 verifier 与 diff-check 通过。03:14:25 客户端启动，77 cube/1,848 UV 顶点与非 FP 逆变换检查通过；40 动作/7,320 帧标准手臂等价和回位检查通过，误差均小于 1e-5。
- ⏳ 用户 Ready/Fire/Reload 三状态视觉未验收通过，数值不代表视觉通过，暂不推进左手精修。指定 bbmodel 已在磁盘确认尺寸减半，旧打开标签需重新加载。无截图/preview/commit/push。

### 31. Native Service Pistol — Right-Hand Ready Grip Recalibration

- 保留上一轮枪体资产 .5、标准 B_skin=1 / Classic 4×12×4 / Slim 3×12×4，不改 camera、不重复缩枪。
- 只调整右 source anchor/reference 与 geo anchor：source origin=(.1,6.761943,8.483114)、rotation=(-43,0,0)；原 carrier 不动，最终手端=(.1,7.75,9.2)、总 X=-65°。掌面倾角从 50° 减至 25°，前臂向下后方，无横向 yaw/roll。
- 左手、Fire/Reload 关键帧/时间、枪体几何/UV/贴图、音效、第三人称、HUD/图标和玩法本轮不改；共享右静态姿态自然传递到其他动作，不做状态特例。
- ✅ compileJava（43s）/processResources（29s）/build（32s）、资产检查和 diff-check 通过；03:30:37 客户端启动，03:31:09 数值检查通过（40 动作/7,320 帧，Classic/Slim/握持漂移均小于 1e-5）。本轮 src 仅 bbmodel、geo 和 DEV 检查变化。
- ⏳ Ready 目视未通过；袖层外壳与 frame 后缘有几何重叠，需检查实际皮肤是否露出穿模。没有数值 PASS 代替握持视觉验收；同一 bbmodel 直接落盘，旧 BB 标签需重新加载。无截图/preview/commit/push。

### 32. Native AFL Gun Framework V0.4.9 — Gun / Player Arm Scale 解耦

- 新gun_model_root位于weapon_root下，只包含gun及机械部件，双手motion/locator在同级。
- gunVisualScale=.8（per weapon）；NativePlayerArmRenderer固定framework style=.246、B_skin=1，去除locator继承的scale/shear，不随枪display/状态缩放原臂。
- 全长Classic/Slim arm+sleeve/真实skin路线，源geometry/UV不改；Fire/Reload原132键保留，只增加4个恒定根scale keys，不重做左手或弹匣轨迹。
- 新`src/main/blockbench/templates/afl_first_person_player_arm_rig.bbmodel`为source-only四组标准臂，canonical origin/-Y/+Z/+X明确。
- ✅ compileJava/processResources/build、源资源验证及diff-check通过；最终compileJava+build46s。04:08:19图形客户端启动，04:08:54完成.6/.8/1.0 gun-scale independence、Classic/Slim等价及40动作/7,320帧数值检查（maxError<1e-5）；原132 keys/77 cubes/UV均保留。
- ⏳ READY PENDING_USER，Reload视觉 NOT_TESTED，实际按键/F5回归待用户；不把数值执行、客户端启动当作视觉通过。
- 第三人称CROSSBOW_HOLD/非FP尺寸补偿、音效、HUD/icon、玩法/TaCZ不改。无截图/preview图片/commit/push。

### 33. 最新资源同步：用户BB参考臂姿态

- 用户已将左臂改回4×12×4；左右reference cube的位置及右臂-17.5°额外旋转等价转到hand_anchor，reference归identity，角点最大误差<4e-9，不另调姿势。
- 右anchor=(.1,8.42206877,1.46570503)/X=-60.5°；左anchor=(-.24988004,9.99753260,-.71193481)/原旋转。源bbmodel与geo/animation同步，资源验证77 cubes/19 bones/136 keys及diff-check PASS；compileJava/processResources/build离线执行46s成功。
- 本次仅导出编译，不启动客户端；此前04:08数值日志不证明此次新姿态，用户确认仅限BB观感。

### 34. 用户 First-Person Display 同步（前次参数记录）

- 仅同步FP right T=(-8.75,.5,0)、left T=(-9.25,.5,0)、共同S=(.76484,.3,.41133)；第三人称/GUI保留runtime原值。
- source/geo/动画不再修改；DEV读取实际display并支持reference编辑形式的内存等价折算、非均匀basis比较。固定framework arm style和生产Renderer不改。
- FP专项资源核对、processResources/build（41s）和diff-check通过；04:35:44短时客户端测试完成，真实Display下Classic/Slim等价/.6-.8-1.0缩枪独立性/40动作回位通过（误差<1e-5）。未做游戏内视觉验收。
- 按用户要求已终止本次测试客户端PID16588；runClient退出-1为主动停止，非编译失败，以后不未经请求启动客户端。

### 35. 用户 First-Person Display 再次同步

- 最新仅更新FP right T=(-8.75,-.25,0)、left T=(-9.25,-.25,0)、共同S=(.5,.5,.5)。其他display/geo/animation/source/生产代码不改。
- FP专项验证、构建资源参数与diff-check通过；processResources/build离线29s成功。
- 04:44:21短时客户端加载新版Display并通过40动作/7,320帧等价及资源检查（误差<1e-5），未做视觉验收；测试后已终止PID26428，runClient退出-1为主动停止。

### 36. Native AFL Gun Framework V0.5 — Runtime Authority Cleanup

- ✅ DEV测试不再控制默认手臂可见性，开发/发布默认均双手；right/both仅显式调试命令。
- ✅ 删除旧ReloadGrip、LegacyHandMapping、旧ArmTrace/Clearance/MatrixChecks/ReloadGripChecks及死方法；保留共享full arm/sleeve、B_skin、矩阵隔离和有效数值工具。
- ✅ 源bbmodel为物理尺寸、Ready双手、Fire/Reload手部与fp_root侧开权威；旧.8烘焙，未重复.5迁移；源与geo/animation/FP Display同步，77 cubes/20 bones/183 keys。
- ✅ Java静态BASE迁入Display，Presentation仅equip；移除JavaReload曲线/.3除数、gunVisualScale覆盖及手臂.246正交化。B_skin固定刚性、Classic/Slim完整几何继承Display。
- ✅ `node tools/export-native-gun.mjs --write/--check`提供保存源文件的可重复导出/核对，仅同步两个FP Display；其他Display/贴图/音效/玩法/HUD/icon保持。
- ⏳ 用户Ready双手、Fire、Reload、重复动作、F5/GUI以及Classic/Slim视觉待验；编译与数值记录见`docs/dev/native-afl-gun-framework-v0.md`，不把数值PASS当视觉PASS。
- V0.4.x以上条目为历史记录，其独立arm scale/单手门控不再是当前行为。无commit/push/截图/preview。

### 37. Native AFL Gun Framework V0.5.1 — Universal Arm Presentation

- 已实现全Native共用最终arm/sleeve scale=(.62,.78,.62)，无按武器/状态覆盖；只抵消输入统一父scale，
  保留animated contact与rotation，不恢复.246/旧正交化。V0.5“尺寸继承Display”由本规则覆盖。
- B_skin/Classic4×12×4/Slim3×12×4/真实skin/sleeve .25与开关不变；固定缩放围绕手端，非中心膨胀。
- 原bbmodel四个export=false参考cube同步presentation显示bounds；枪、locator、motion、Display、动画、音效、第三人称均冻结。
- compileJava/processResources/build离线通过；统一参考场景Slim右臂长度311→379px、cap宽95→144px，数值不等于视觉PASS。
- ✅ 07:08:32新版图形客户端已启动；五种parent scale、真实Classic/Slim arm/sleeve、40动作/7,320帧检查通过，
  contact drift=3.414285e-8；release DEV classes=0。用户回复“目前没有问题”，Ready/Fire本轮通过，Reload功能无已知回归。
  Reload本轮只做功能回归，深度/展开方向留V0.5.2；详见framework文档。无commit/push/截图/preview图片。

### 38. Native AFL Gun Framework V0.5.2 — Ready Aimline / Reload Entrance

- 已按保存bbmodel的muzzle/barrel -Z和sight投影校准FP Display旋转；右context参考准心偏差
  (+54.39,+59.13)px→约(0,0)px。不是ADS，双手接触/通用arm尺寸/枪geometry均冻结。
- 仅fp_root前.24秒改为加密linear缓入，.08秒进度25.93%→5.23%；.24秒后局部路径、
  weapon_root、左右手/弹匣轨道、1.30秒总长和.40/.95秒声音时序不变。
- 源animation/FP Display同步runtime；geo保留原字节，既有格式/guard精度差异不顺手改动。
- 修复导出"1"秒整数键抢排导致Gecko时段错误；改为"1.0"并验证顺序，DEV对照源与实际求值。
  .24秒后源路径未改，但旧错误Runtime时序随导出修正，需重新验收中后段。
- compileJava/processResources/build离线通过，release DEV classes=0；07:35最终图形客户端启动，
  真正Gecko/source早期同秒检查、40动作/7,320帧和Classic/Slim完整臂/袖层对应通过。
  用户Ready/Fire/起手重复动作及中后段验收PENDING_USER。
  07:36进入test世界，07:37正常保存退出，最终测试进程已结束；尚无用户视觉结论。
  数值不代表视觉通过；中后段深度/方向不是本轮重做范围。详见framework V0.5.2。
- 无commit/push/截图/preview文件；DEV增加真实Gecko枪线和早期速度采样。

### 39. Service Pistol Reload Composition V2 — Blockbench PASS / Runtime已同步

- 用户明确确认V2 Blockbench正常速度预览通过；本轮保留bbmodel，不再设计构图。
- 仅导出Runtime animation.json，Ready/Fire、geo、Display、全部Java与音效未改。
  1.30s与.40/.95声音节点保持；第三人称只共享新magazine轨道，专用Renderer/姿态/Display冻结。
- compileJava/processResources/build通过；runClient已启动（本轮PID41356）。
- 08:32:45 Gecko实际评估40次动作/7,320帧PASS，Classic/Slim等价与右握持回位检查通过。
- ⏳ 完整Reload实机视觉PENDING_USER：起手、.40抽匣、.95插入、1.10–1.30回位和重复动作。
  不把Blockbench或数字通过当成实机目视通过。测试后保存退出/结束本次客户端。
- geo存在旧格式及单个.00001尺寸舍入差异，未重导；animation使用精确源导出定向校验。
  无截图/preview文件/commit/push。详细证据见framework文档V2章节。

### 后续功能（未实现，计划保留）

- **9mm Ammo Assets / 弹药资产**
    - `9mm_round`：完整 9mm 弹药物品/模型
    - `9mm_casing`：空弹壳模型，只用于客户端抛壳
    - 正式物品图标、名称、本地化
    - 暂时不要做一堆弹药种类，先只有普通 9mm
- **Real Ammo State / 真正弹药状态**
    - Service Pistol 弹匣容量，例如 `17`
    - 当前弹量写进枪械 ItemStack 数据
    - 服务器权威
    - 丢枪、换槽、死亡、重新进世界后不能莫名重置
    - R 从背包消耗 `9mm_round`
    - Creative 可以有单独测试逻辑
    - 把现在的 infinite ammo 原型正式关掉
- **Gun HUD / 枪械 HUD**
    - 例如右下角：  
        `17 / 64`
    - 前者当前弹匣，后者背包可用 9mm
    - Reload 时可以显示状态
    - 空仓明显反馈
    - 不要先做复杂 TaCZ 风格 GUI，先做 AFL 自己简洁 HUD
- **Empty State + Slide Lock / 空仓挂机**
    - 最后一发射出
    - `ammo = 0`
    - slide 停在后方
    - 不能继续产生有效射击
    - 左键只触发 `dry_fire.ogg`
    - 无枪口火焰
    - 无抛壳
    - 无伤害
    - 无 Noise/Tinnitus 枪声事件
- **Tactical Reload + Empty Reload / 两种换弹**
    - `ammo > 0`：  
        `mag_out → mag_in → ready`  
        不动套筒
    - `ammo == 0 / slide locked`：  
        `mag_out → mag_in → slide_action → slide forward → ready`
    - 这一步正好正式把 `slide_action.ogg` 用起来
    - 后面模型师也可以帮我们 polish 这两个动画
- **Shell Ejection / 抛壳**
    - 使用现成 `ejection_anchor`
    - 每次真正发射一发才生成
    - client-only
    - 向右上方随机抛出
    - 自转
    - 有简单重力
    - 碰撞地面后播放 `shell_casings_dropping.ogg`
    - 短暂存在然后消失
    - 不生成真实 ItemEntity，不污染世界、不存档
- **Fire State 完整化**
    - Fire cooldown
    - Reload 时禁止射击
    - Empty 时 dry fire
    - 切换物品时正确取消/结束 reload
    - 死亡/丢弃/换维度不能卡在 `RELOADING`
    - 连点不能绕过 RPM
    - 这一步把枪械状态机真正立住：  
        `READY / FIRING / RELOADING / EMPTY_LOCKED`
- **然后才接真正战斗逻辑**
    - Hitscan
    - Damage
    - Range
    - head/body hit 判定先保持简单
    - 基础 recoil
    - muzzle flash
    - Noise
    - Tinnitus
