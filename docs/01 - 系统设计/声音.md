# 声音与感染者听觉

## 1. 系统概述

声音系统把服务器上的玩家行为和爆炸转换为带世界坐标、类型及传播半径的 `NoiseEvent`。感染者位于经方块声学衰减后的有效范围内时，会把调查位置更新为真正的声源位置。

当前事件类型包括 `GUNSHOT`、`EXPLOSION`、`BLOCK_BREAK`、`FOOTSTEP`、`LANDING` 和 `INTERACTION`。系统不额外生成感染者，也不改变声音半径、目标选择或听觉记忆时长；噪声广播循环本身只更新听觉与破障授权状态，不在广播循环中直接执行 `createPath`。

如果声音的传播半径超过 10，感染者会尝试朝声源所在位置移动，并尝试破坏路上的易碎阻碍。

为避免大群感染者在同一 tick 同步寻路，首次调查路径按实体 UUID 稳定错开 `0–9 ticks`；成功路径最短 `20 ticks` 后刷新，失败路径冷却 `30–40 ticks`。距离现有目标不超过 2 格的连续声音只刷新最后事件时间，不重置调查 / 搜索阶段或稳定目标 revision；声音位置明显变化时仍会建立新目标。入口搜索为每实体 `40 ticks` 冷却并带 `0–19 ticks` 初始错峰，每轮只对评分最高的至多 8 个候选建立路径。

后续候选：

- [ ] 各种机器
- [ ] 玩家吃食物
- [ ] 下雨降低声音传播
- [ ] 雷雨进一步干扰声音
- [ ] 不同感染者听觉能力差异

---

## 2. 基础声音半径

| 活动 | 声音半径（格） |
| --- | ---: |
| 潜行 | 2 |
| 走路 | 4 |
| 冲刺 | 8 |
| 普通落地 | 5 |
| 较重落地 | 8 |
| 很重落地 | 12 |
| 开关木门 | 4 |
| 开关铁门 | 6 |
| 木活板门 | 3 |
| 铁活板门 | 5 |
| 栅栏门 | 3 |
| 打开箱子 | 3 |
| 打开木桶 | 3 |
| 放置方块 | 4 |

| 破坏方块 | 声音半径（格） |
| --- | ---: |
| 软质方块：泥土、草、沙、雪等 | 4 |
| 木质方块 | 6 |
| 石质方块 | 8 |
| 玻璃 | 10 |
| 金属 | 12 |

---

## 3. 枪声：内部入口保留，尚未接 Native Shot

本轮无TaCZ隔离服务器44/44测试通过，覆盖保留的Noise/耳鸣/Explosion核心；
带/不带TaCZ客户端均成功启动并退出。两者都有同一Ready枪线DEV断言失败，
不算完整手枪视觉通过；详见 `docs/dev/native-afl-gun-framework-v0.md` 本轮验证结果。

TaCZ 不再是 AFL 强制依赖，`mods.toml` 不再声明 TaCZ；Gradle 仅保留
`runtimeOnly fg.deobf("curse.maven:timeless-and-classics-zero-1028108:8141310")` 供开发对比。
`-PaflWithoutTacz` 可省略此开发依赖。Native Gun Framework 是后续枪械功能唯一生产路线。
已删除 `TaczNoiseEvents`、`GunshotNoiseResolver`、枪械ID/类别fallback、附件silence读取及旧专用审计器。
TaCZ 开火不再产生 AFL 枪声 Noise / Tinnitus；但任何模组走标准 Forge Detonate 的爆炸
仍按通用爆炸入口处理，不屏蔽或重复监听第三方弹丸。

保留 NoiseSystem、感染者听觉、Explosion Noise、Explosion Tinnitus、共享Episode、Overlay/音频、
GunshotExposureTracker/Profile/Accumulator 的通用参数与冷却。未来 successful shot 的接入位置：
`NoiseSystem.emit(NoiseEvent, ServerLevel)`（类型 `GUNSHOT`）及
`GunshotExposureTracker.onGunshot(level, shooter, sourcePosition, acousticRadius, false)`。
后者仅发耳鸣impulse、不代发Noise；布尔参数是保留的通用策略输入，不再读取外部附件。
当前 Native Service Pistol 尚未调用这两个枪声入口；本轮未实现枪声pipeline或Native消音器。
Service Pistol 视觉/动画/声音、9mm资产、HUD及弹药逻辑未改。

删除专用TaCZ开发测试；保留爆炸、耳鸣核心测试，并用显式数值输入测试内部Noise听觉入口。
旧TaCZ测试通过记录仅属历史，不能视为当前集成仍存在或本轮运行验证。

通用枪声暴露调参仍保留在 `GunshotTinnitusProfile`：16格范围，loudness=clamp((R−64)/96,0,1)，
距离衰减=(1−d/16)²，单发基础系数0.55，R≥128乘1.25、R≥160再乘1.35，上限0.75。
累积上限1.5、衰减0.008/tick，阈值0.35/0.60/0.90；6tick硬冷却、20tick延长节流、发送后保留45%。
这些是未接入Native的通用核心，不是当前TaCZ枪械规则。

## 4. 爆炸：Noise System V2

所有进入 Forge `ExplosionEvent.Detonate` 的真实服务端爆炸都会产生一次 `EXPLOSION` Noise Event。事件位置直接使用 `Explosion.getPosition()`；强度读取 live `Explosion` 的实际 radius/strength 字段，不按 TNT、手雷或弹头类型建立硬编码表。

动态半径公式：

```text
rawRadius = 96 + explosionStrength * 24
roundedRadius = round(rawRadius / 8) * 8
noiseRadius = clamp(roundedRadius, 96, 256)
```

| Explosion strength | Noise radius（格） |
| ---: | ---: |
| 2.0 | 144 |
| 2.5 | 160 |
| 3.0 | 168 |
| 4.0（原版 TNT） | 192 |
| 5.0 | 216 |
| 6.0 | 240 |
| 约 6.67 或更高 | 256（上限） |

仅监听 `Detonate`，因此同一实际爆炸不会因同时处理 `Start` 和 `Detonate` 而重复产生噪声。未来爆炸物只要进入标准 Minecraft/Forge Explosion 流程，就会自动使用同一公式。

Explosion Noise 与 Explosion Tinnitus 是两个独立消费者：前者决定感染者听觉范围 `96–256` 格；后者继续使用自己的玩家距离、严重度、音频与 Overlay 计算。两者可以响应同一个 `Detonate`，但互不借用半径或状态。

---

## 5. 声音阻隔

| 方块 | 效果 |
| --- | ---: |
| 羊毛 | 每穿过 1 层完整羊毛方块，有效传播半径 × 0.5 |
