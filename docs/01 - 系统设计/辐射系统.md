# 辐射系统

## 0. 一眼看懂

辐射系统现在已经能形成完整的基础生存压力：世界不同区域有不同环境辐射，建筑可以削弱辐射，玩家会累计 Dose，并随 Dose 升高进入辐射病 I–IV。

本页记录的是当前 live code。此次只做文档审计，没有启动游戏，因此“已实现”表示代码已经接入，不新增本轮实机验证结论。

| 状态  | 功能                      | 现在做到哪                     |
| --- | ----------------------- | ------------------------- |
| ✅   | 世界辐射场                   | 世界种子决定稳定的二维辐射分布           |
| ✅   | 环境辐射                    | 根据区域换算为 `0–240 RU/h`      |
| ✅   | 建筑屏蔽                    | 钢筋混凝土通过 14 方向采样削弱环境辐射     |
| ✅   | Dose                    | 每秒累计，保存到玩家数据，死亡不清除        |
| ✅   | Residual                | 在污染区缓慢积累，在 SAFE 区缓慢衰减     |
| ✅   | Radiation Sickness      | Dose 达到阈值后进入 I–IV，并施加对应惩罚 |
| ✅   | Geiger                  | 手持时显示读数、Dose、区域并播放点击声     |
| ✅   | Radiation Heart Overlay | II–IV 阶段逐渐替换已填充的生命心外观     |
| ✅   | Radiation Atmosphere    | 辐射越高，空气雾色和可视距离变化越明显       |
| ✅   | Item Contamination V1A  | ItemStack 离散污染数据、Tooltip 与调试命令 |
| 🟡  | `localRadiation`        | 接口存在，但当前固定为 `0 RU/h`      |
| ⏳   | 污染物自动生成与局部放射源            | 尚未接入掉落物、战利品和背包物品辐射       |
| ⏳   | 洗消与 Dose 治疗             | 尚未实现正式玩法；目前只有管理员命令可改数据    |

当前最重要的后续工作：让方块掉落物和世界战利品使用 Item Contamination V1A 数据（V1B），之后再确定各等级对应的局部辐射率。Residual 是否进入 Dose、正式洗消与 Dose 治疗仍需单独设计。开局生态区在 `d=160` 附近还有一次已知的不连续跳变，也需要单独修复。

## 1. 三个核心概念

### Environment Radiation

简单理解：**你现在站的这个地方有多危险。**

- 单位：`RU/h`
- 来源：世界辐射场、地表群系、安全区和开局生态区共同决定。
- 建筑屏蔽会降低玩家实际受到的环境辐射。
- 当前自然环境范围是 `0–240 RU/h`。

### Dose

简单理解：**你身体已经累计吃进去多少辐射。**

- 单位：`RU`
- 每秒根据玩家当前真正受到的辐射增加。
- 不会自然下降。
- 死亡后仍然保留。
- Dose 决定辐射病等级。

### Residual

简单理解：**你身上现在残留了多少污染。**

- 当前显示单位：`RU/h`
- 在非 SAFE 区域缓慢靠近环境对应的目标值。
- 在 SAFE 区域缓慢衰减。
- 死亡后清零，正常退出和重新登录会保留。
- 目前只作为独立污染读数显示在 Geiger 上，**尚未计入 Dose**。

### Local Radiation

简单理解：附近方块、物品或随身污染物直接造成的局部辐射。

接口已经存在，但当前代码固定返回：

```text
localRadiation = 0 RU/h
```

因此现在还没有“背着放射性物品也持续受辐射”的玩法。

### Item Contamination V1A

这是物品堆自身的离散污染数据，与玩家的 Residual 是两套独立状态。污染等级保存在 ItemStack NBT 的 `AflContaminationLevel` 字节中。

| 数值 | 等级 | 中文显示 | NBT 行为 |
| ---: | --- | --- | --- |
| `0` | `CLEAN` | 洁净 | 不写入该 NBT；清理后若没有其他 NBT，则恢复为无 Tag ItemStack |
| `1` | `TRACE` | 微量 | 写入 byte `1` |
| `2` | `LOW` | 轻度 | 写入 byte `2` |
| `3` | `MODERATE` | 中度 | 写入 byte `3` |
| `4` | `HIGH` | 高度 | 写入 byte `4` |
| `5` | `SEVERE` | 严重 | 写入 byte `5` |

同一个 ItemStack 整堆共享一个污染等级。相同物品、相同污染 NBT 可以按原版规则堆叠；污染等级不同会因为 NBT 不同而分开。Level 0 不添加无意义 NBT，因此洁净物品与原版堆叠兼容。

代码提供了从环境暴露值到目标污染等级的纯映射，但 V1A 尚未调用它自动污染物品：

| 输入值 | 目标等级 |
| ---: | --- |
| `<= 0.000001` | `CLEAN` |
| `< 1` | `TRACE` |
| `< 10` | `LOW` |
| `< 60` | `MODERATE` |
| `< 150` | `HIGH` |
| `>= 150` | `SEVERE` |

目前只有管理员调试命令会写入或清除污染。Level 1–5 的物品 Tooltip 会显示现有辐射图标和本地化污染等级；Level 0 不增加 Tooltip。

V1A 明确没有实现：

- 方块掉落、世界战利品或 ItemEntity 自动获得污染；
- 背包内污染物贡献 `localRadiation`；
- Item Contamination 与玩家 Residual 相互转换；
- 各污染等级对应的精确 `RU/h`；
- Geiger 对单件污染物显示精确源强。

## 2. 玩家实际会遇到什么

### 四个辐射区域

| 区域 | 环境场 `F` | 理论环境辐射率 | 玩家感受 |
| --- | ---: | ---: | --- |
| `SAFE` | `< 0.08` | `0 RU/h` | 环境本身不增加 Dose；Residual 会缓慢衰减 |
| `IRRADIATED` | `0.08–<0.62` | `1–<10 RU/h` | 长时间暴露才会形成明显 Dose |
| `HEAVY_FALLOUT` | `0.62–<0.84` | `10–<60 RU/h` | Dose 增长明显，长时间停留很危险 |
| `EXTREME` | `>= 0.84` | `60–240 RU/h` | 极危区域，无遮蔽暴露会很快进入高阶段辐射病 |

区间边界以代码比较规则为准。例如 `F = 0.62` 已经属于 `HEAVY_FALLOUT`，`F = 0.84` 属于 `EXTREME`。

### 为什么焦土特别危险

焦土使用 `EXTREME` 生物群系 Profile。它会把自然场约束到 `F = 0.86–1.00`，对应约 `82.5–240 RU/h`，所以焦土不是只有视觉危险。

在完全无遮蔽、读数恒定为 `240 RU/h` 时：

- 约 `30 秒`累计到 `2 RU`，进入辐射病 I。
- 约 `5 分钟`累计到 `20 RU`，进入辐射病 IV。

建筑屏蔽可以显著延长这些时间，但不会清除已经累计的 Dose。

### 建筑能保护什么

建筑屏蔽只降低实际进入玩家的环境辐射。

简单理解：

```text
玩家当前真正受到的辐射
=
环境辐射经过建筑屏蔽后的部分
+
附近局部放射源
```

当前公式：

```text
finalRadiation
=
ambientRadiation × shieldingTransmission
+
localRadiation
```

因为目前 `localRadiation = 0`，所以现阶段通常等价于：

```text
finalRadiation = ambientRadiation × shieldingTransmission
```

注意：建筑会降低 `finalRadiation`，但通常不会把 HUD 上的区域枚举从 `EXTREME` 改成 `SAFE`。区域表示外部环境类型，屏蔽表示玩家实际吃到多少辐射。

## 3. Dose 怎么增长

服务器每 `20 ticks`（1 秒）结算一次。

简单理解：每秒吸收“每小时辐射率的 1/3600”。

```text
newDose = oldDose + finalRadiation / 3600
```

| 恒定 `finalRadiation` | 每秒增加 Dose | 达到 `2 RU` | 达到 `20 RU` |
| ---: | ---: | ---: | ---: |
| `1 RU/h` | `0.000278 RU` | 2 小时 | 20 小时 |
| `10 RU/h` | `0.002778 RU` | 12 分钟 | 2 小时 |
| `20 RU/h` | `0.005556 RU` | 6 分钟 | 1 小时 |
| `60 RU/h` | `0.016667 RU` | 2 分钟 | 20 分钟 |
| `240 RU/h` | `0.066667 RU` | 30 秒 | 5 分钟 |

当前规则：

- Dose 只使用 `sample.finalRadiation()`。
- Residual 当前不进入 Dose。
- Dose 不会自然下降。
- 正常存档、重新登录和死亡重生都会保留 Dose。
- 尚无正式医疗玩法降低 Dose；管理员命令可以修改或清空。

## 4. Residual 怎么增长和消退

### 在非 SAFE 区域

先根据当前 `finalRadiation` 计算目标值，最高为 `12 RU/h`：

```text
targetResidual = min(12, finalRadiation × 0.05)
```

然后每秒向目标靠近 `0.2%`：

```text
newResidual
=
oldResidual + (targetResidual - oldResidual) × 0.002
```

| `finalRadiation` | `targetResidual` |
| ---: | ---: |
| `20 RU/h` | `1 RU/h` |
| `60 RU/h` | `3 RU/h` |
| `120 RU/h` | `6 RU/h` |
| `240 RU/h` | `12 RU/h` |
| `>240 RU/h` | 仍封顶为 `12 RU/h` |

这是双向逼近：从高辐射区进入较低但仍非 SAFE 的区域后，Residual 会缓慢向更低目标回落。

### 在 SAFE 区域

SAFE 区每秒独立衰减：

```text
newResidual = oldResidual × 0.9995
```

低于 `0.01 RU/h` 时直接归零。

| 情况 | Dose | Residual |
| --- | --- | --- |
| 保存、退出、重新登录 | 保留 | 保留 |
| 非死亡实体 Clone | 保留 | 保留 |
| 死亡后重生 | 保留 | 清零 |

## 5. Radiation Sickness I–IV

辐射病完全由累计 Dose 决定，惩罚由服务端执行。

| Dose | Stage | 移速 | 收到的治疗 | 反胃 | 周期伤害 |
| ---: | ---: | ---: | ---: | --- | --- |
| `< 2 RU` | 0 | 无 | `100%` | 无 | 无 |
| `2–<5 RU` | I | `-5%` | `100%` | 无 | 无 |
| `5–<10 RU` | II | `-10%` | `75%` | 无 | 无 |
| `10–<20 RU` | III | `-15%` | `50%` | 持续 10–12 秒；结束后冷却 45–75 秒 | 无 |
| `>= 20 RU` | IV | `-20%` | `25%` | 持续 16–24 秒；结束后冷却 25–45 秒 | 每 5 秒受到 1 点魔法伤害（半颗心） |

当前细节：

- 移速使用临时 `MULTIPLY_TOTAL` Attribute Modifier。
- 治疗削弱修改玩家实际收到的治疗量。
- III/IV 的反胃持续时间和冷却以 1 秒为随机步长。
- 冷却从上一次反胃结束后开始计算。
- IV 周期伤害和反胃相互独立。
- 自定义 `radiation_sickness` Effect 负责显示阶段，真正惩罚由服务端管理器执行。
- Effect 没有可用的 curative item，因此牛奶不能解除。
- 死亡会清除临时状态，但 Dose 会保留，辐射病随后按原 Dose 恢复。

> 牛奶不能解除辐射病；死亡也不能清除累计 Dose。

## 6. 玩家反馈

### Geiger HUD 与声音

只有主手或副手持有盖革计数器时，才同步并显示完整仪器数据。

| 当前区域 | 仪器使用的读数 |
| --- | --- |
| `SAFE` | `residualRadiationRate` |
| 非 `SAFE` | `finalRadiation` |

HUD 显示当前辐射/Residual、累计 Dose 和区域。

| 测量值 | HUD |
| ---: | --- |
| `< 20 RU/h` | 显示两位小数 |
| `>= 20 RU/h` | 显示“超量程” |

`20 RU/h` 只是 Geiger 量程上限，不会裁剪真实辐射、Dose 或 Residual 计算。

点击声控制点：

| 测量值 `RU/h` | 目标 CPS |
| ---: | ---: |
| `0.00` | `0.00` |
| `0.10` | `0.25` |
| `0.50` | `0.75` |
| `1.50` | `2.00` |
| `3.00` | `4.00` |
| `6.00` | `7.00` |
| `9.00` | `10.00` |
| `12.00` | `12.00` |
| `20.00+` | `20.00` |

控制点之间线性插值；客户端每 tick 按 `CPS / 20` 概率播放一次独立点击。

### Radiation Heart Overlay

心形覆盖只替换已经填充的原版生命心外观，不改变真实生命值。

| Stage | 被替换的已填充心 | 外观 |
| ---: | ---: | --- |
| 0–I | `0%` | 原版 |
| II | 约 `30%` | Mild |
| III | 约 `60%` | Severe |
| IV | `100%` | Critical |

选择结果由玩家 UUID 和心形索引稳定决定，不会每帧随机跳动；半颗心使用对应 half 纹理。

### Radiation Atmosphere

空气雾效使用同步后的 `finalRadiation`，在 `10 RU/h` 达到最大目标强度。

```text
x = min(1, finalRadiation / 10)
targetIntensity = x² × (3 - 2x)
```

- 客户端每 tick 向目标强度靠近 `10%`。
- 最大强度时，雾色以 `0.48` 强度向 RGB `(0.43, 0.46, 0.34)` 混合。
- 远裁剪面最多缩短到原来的 `70%`。
- 玩家位于液体雾中时不应用空气雾修改。
- 视觉强度封顶不代表真实辐射封顶。

## 7. 当前实现边界

| 状态 | 功能 |
| --- | --- |
| ✅ | 确定性世界辐射场 |
| ✅ | 生物群系辐射约束 |
| ✅ | 出生点/地堡安全锚点 |
| ✅ | 钢筋混凝土建筑屏蔽 |
| ✅ | Dose 累计与持久化 |
| ✅ | Residual 增长、回落与 SAFE 衰减 |
| ✅ | 辐射病 I–IV |
| ✅ | Geiger HUD 与点击声 |
| ✅ | 辐射心形覆盖 |
| ✅ | 辐射环境雾效 |
| ✅ | Item Contamination V1A：ItemStack 等级、Tooltip、调试命令 |
| 🟡 | `localRadiation` 接口存在，当前恒为 0 |
| ⏳ | 方块掉落与世界战利品自动污染（V1B） |
| ⏳ | 背包污染物或方块作为局部辐射源 |
| ⏳ | Residual 作为额外来源进入 Dose |
| ⏳ | 正式洗消玩法 |
| ⏳ | 正式降低 Dose 的医疗玩法 |

## 8. 已知问题

### 开局核心边界不连续

开局生态区辐射上限在距离世界坐标原点约 `d=160` 的边界存在一次不连续跳变：

- `d <= 160` 直接使用 `0.079` 上限。
- 刚越过 160 后进入下一段从接近 `0` 开始的 smoothstep。

因此这里目前不是完全连续的平滑过渡。尚未修复。

### Radiation Field 与 biome placement 不是同一个场

辐射使用独立的 `RadiationField`；地表群系使用 Minecraft climate sampler 与 TerraBlender。`BiomeRadiationResolver` 会在群系确定后约束辐射范围，但当前不是“同一个 F 精确决定群系”。

### 当前污染玩法仍是半成品

Residual 已经能记录、持久化和显示，但还不会增加 Dose，也没有正式洗消方式。Item Contamination V1A 已经能在 ItemStack 上保存离散等级，并显示 Tooltip、接受管理员命令，但不会自动污染掉落物或战利品。`localRadiation` 仍固定为 0，所以携带污染物暂时没有实际后果。

## 9. 下一步

以下都属于计划，不是当前已实现行为：

1. 实现 Block Drops / World Loot Contamination V1B，让自然获得的 ItemStack 使用现有离散污染数据。
2. 定义污染等级对应的局部辐射率，并接入背包或附近物品辐射源。
3. 明确 Residual 是否以及如何进入 Dose。
4. 增加正式洗消玩法。
5. 增加降低 Dose 的医疗玩法。
6. 单独修复开局生态区 `d=160` 的不连续跳变。

## 10. 技术细节

这一部分主要供开发和调试时查阅。

### 10.1 完整计算顺序

```text
世界种子二维辐射场
    ↓
地表生物群系约束
    ↓
安全锚点压制
    ↓
开局生态区辐射上限
    ↓
环境场 F → ambientRadiation
    ↓
14 方向遮蔽采样 → shieldingTransmission
    ↓
finalRadiation
= ambientRadiation × shieldingTransmission + localRadiation
```

- `RadiationZone` 根据屏蔽前的有效环境场判断。
- 安全锚点核心内且 `localRadiation <= 0` 时，强制为 `SAFE / 0 RU/h`。
- 非主世界维度返回 `SAFE / 0 RU/h`；Residual 按 SAFE 规则衰减。

### 10.2 原始 Radiation Field

`RadiationField` 使用世界种子生成确定性二维 value noise，只依赖 X/Z：

```text
rawF = clamp01(broad × 0.82 + detail × 0.18)
```

| 层 | 空间尺度 | 权重 |
| --- | ---: | ---: |
| Broad | `768 blocks` | `0.82` |
| Detail | `192 blocks` | `0.18` |

### 10.3 生物群系约束

| 地表群系 | Profile | 约束后的 `F` | 约辐射率 |
| --- | --- | ---: | ---: |
| 原版平原 | `SAFE` | `0–0.075` | `0 RU/h` |
| 污染疏林 | `IRRADIATED` | `0.10–0.58` | `1.33–9.33 RU/h` |
| 沉降荒原 | `HEAVY_FALLOUT` | `0.62–0.82` | `10–55.45 RU/h` |
| 焦土 | `EXTREME` | `0.86–1.00` | `82.5–240 RU/h` |
| 未识别/其他群系 | `UNKNOWN` | 保留 `0–1` | `0–240 RU/h` |

`BiomeRadiationResolver` 只读取已加载 chunk；不会为了远处采样强制生成或加载 chunk。每个世界最多缓存 `4096` 个 X/Z 解析结果。

### 10.4 安全锚点

安全锚点初始来自世界出生点；地堡成功生成并绑定后可改为地堡内指定安全点。

| 距离安全锚点 | 压制系数 |
| --- | ---: |
| `d <= 40` | `0` |
| `40 < d < 96` | `smoothstep((d - 40) / 56)` |
| `d >= 96` | `1` |

```text
preStartupF = biomeF × anchorSuppression
```

### 10.5 开局生态区上限

开局生态区以世界坐标 `(0, 0)` 为中心：

| 区域 | 上限行为 |
| --- | --- |
| 核心平原 `d <= 160` | 上限 `0.079` |
| 核心外至噪声化平原边界 | 从 `0` smoothstep 到 `0.079` |
| 平原边界至污染疏林外边界 | 从 `0.10` 过渡到 `0.42` |
| 疏林边界后的 `48 blocks` | 从 `0.42` 交接回原有效场 |
| 交接区之外 | 不施加上限 |

```text
effectiveF = min(preStartupF, startupCap)
```

仅在 `startupCap` 存在时应用。`d=160` 的已知不连续见“已知问题”。

### 10.6 `F` 到 `RU/h`

```text
F < 0.08:
R(F) = 0

0.08 <= F < 0.62:
R(F) = 1 + 9 × (F - 0.08) / 0.54

0.62 <= F < 0.84:
R(F) = 10 + 50 × (F - 0.62) / 0.22

F >= 0.84:
R(F) = 60 + 180 × (F - 0.84) / 0.16
```

换算前把 `F` 限制到 `0–1`，所以自然环境最大为 `240 RU/h`。

### 10.7 14 方向建筑屏蔽

- 6 个正交方向和 8 个空间对角方向。
- 每条射线最多检查 8 个采样位置。
- 每命中一个 `apocalypse_firstlight:radiation_shielding` Tag 方块，透射率乘以 `0.35`。
- 单条射线透射率低于或等于 `0.01` 后停止。
- 14 条射线透射率取平均。
- 结果按玩家 BlockPos 缓存 `10 ticks`。

```text
rayTransmission = 0.35 ^ hitCount
shieldingTransmission = sum(all 14 rays) / 14
shieldingPercent = 1 - shieldingTransmission
```

当前 Tag 内的正式材料是：

| 方块 | Registry ID | 单层透射率 | 单层削弱 |
| --- | --- | ---: | ---: |
| 钢筋混凝土 | `apocalypse_firstlight:reinforced_concrete` | `0.35` | `65%` |

### 10.8 同步与 stale

- 基础 `finalRadiation` 每 `10 server ticks` 同步给主世界玩家，用于环境视觉。
- 只有手持 Geiger 时才同步完整仪器数据。
- Geiger 快照超过 `40 ticks` 未更新后视为 stale，不继续作为有效读数/声音来源。

## 11. 管理员 / DEV 调试

辐射环境、Dose 和 Residual 命令以 `/afl radiation` 开头；物品污染命令以 `/afl contamination` 开头。全部需要权限等级 2。

| 命令 | 用途 |
| --- | --- |
| `/afl radiation here` | 输出当前位置世界场、群系约束、安全区、屏蔽和最终读数 |
| `/afl radiation dose` | 查看当前辐射率与 Dose |
| `/afl radiation dose add <amount>` | 增加 Dose |
| `/afl radiation dose set <amount>` | 设置 Dose |
| `/afl radiation dose reset` | 清空 Dose |
| `/afl radiation residual` | 查看 Residual |
| `/afl radiation residual set <amount>` | 设置 Residual |
| `/afl radiation residual reset` | 清空 Residual |
| `/afl radiation locate natural_safe [maxRadius]` | 搜索自然 SAFE 区域 |
| `/afl radiation locate irradiated [maxRadius]` | 搜索自然轻度辐射区 |
| `/afl radiation locate heavy_fallout [maxRadius]` | 搜索自然重度沉降区 |
| `/afl radiation locate extreme [maxRadius]` | 搜索自然极危辐射区 |
| `/afl contamination get` | 查看主手 ItemStack 的污染等级 |
| `/afl contamination set <0..5>` | 设置主手整堆物品的污染等级；`0` 等同清除 |
| `/afl contamination clear` | 清除主手整堆物品的污染 NBT |

## 12. 主要代码位置

| 模块 | 文件 |
| --- | --- |
| 世界场、区域、换算和最终采样 | `RadiationManager.java` |
| 确定性二维噪声 | `RadiationField.java` |
| 生物群系约束 | `BiomeRadiationResolver.java`、`BiomeRadiationProfile.java` |
| 建筑屏蔽 | `RadiationShielding.java` |
| Dose 与 Residual 每秒结算 | `RadiationExposureEvents.java` |
| Dose/Residual 持久数据 | `RadiationExposureData.java` |
| 辐射病阶段和惩罚 | `RadiationSicknessManager.java`、`RadiationSicknessEvents.java` |
| Geiger HUD | `GeigerHudOverlay.java` |
| Geiger 点击声 | `GeigerSoundController.java` |
| 辐射心形覆盖 | `RadiationHeartOverlay.java` |
| 环境雾效 | `RadiationAtmosphere.java`、`RadiationAtmosphereClient.java` |
| 服务端同步 | `RadiationSyncEvents.java`、`AflNetwork.java` |
| 管理员命令 | `RadiationCommands.java` |
| ItemStack 污染数据与等级映射 | `ItemContamination.java` |
| 物品污染 Tooltip | `ClientContaminationTooltip.java` |
| 物品污染管理员命令 | `ContaminationCommands.java` |
