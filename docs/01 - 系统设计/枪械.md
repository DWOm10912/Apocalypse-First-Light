# 枪械

## Native Gun V0.6 — 9mm / 弹匣 / 功能HUD（已实现，部分验证边界见framework）

`apocalypse_firstlight:9mm_round` 已注册为64堆叠普通Item，位于创造标签Service Pistol之后，
中英文名9mm手枪弹/9mm Round，复用既有3D物品模型。Casing仍只保留资产，没有抛壳实现。
`NativeGunDefinition`/`NativeGunItem`提供id、ammoType、capacity、hudIcon、26tick时长、19tick装填点、3tick开火间隔。
Service Pistol容量17，无chamber/17+1；每把枪NBT `AflGunAmmo.ammoInMagazine` 范围0..17。
无状态的新枪逻辑默认17，服务端首次物品tick/开火持久化；已有0绝不自动补满。
服务端每次获准开火扣1，0发不播放正常动画/声音，无dry-fire。
Reload满匣/零reserve拒绝，19tick重新统计背包并一次性扣弹补匣，26tick结束；8tick抽匣音不改。
当前枪对象/槽位/维度变化、死亡、旁观会取消；已提交的装弹不退回、不再提交。
背包所有原版槽位（含副手）按definition.ammoType计数；不读取其他容器或嵌套背包。
所有模式包括Creative均真实扣弹，不实现无限弹特例。状态通过原版ItemStack/Inventory同步，HUD无独立缓存。

用户明确取消背景框：HUD仅复用925×574剪影 `textures/gui/gun/service_pistol_hud.png` 和突出current/次级reserve数字。
767×524渲染图另存 `textures/item/service_pistol_inventory.png`，仅供背包图；不覆盖128×128枪体UV贴图。
HUD右下GUI坐标布局，当前主手实现NativeGunItem时显示，其他物品/隐藏HUD/旁观时不显示。
实机首轮发现剪影过大，已从98×60收至36×22 GUI单位；坐标(width-70,height-99)，
current字号1.25、reserve字号1，无背景，底部留空间避开默认盖革计/HUD。
ServicePistolItem通过Forge shouldCauseReequipAnimation忽略仅弹量NBT更新/首次GeckoLibID初始化；
换槽、不同枪身份及其它NBT变化仍触发装备动作，避免每次扣弹让整枪下沉再抬起。未改Fire动画。
构建及48项GameTest通过；紧凑HUD截图已目视核对，用户确认“现在不跳了”。全部持久化实机操作与其它GUI Scale仍未完整覆盖。
手枪源、Geo、Fire/Reload动画、Display、臂渲染规则和音效资源均冻结；GUI物品图按用户新授权替换。
没有Hitscan/Damage/Recoil/MuzzleFlash/抛壳/ADS/消音器，也未调用Native Noise/Tinnitus入口。
下一阶段V0.6.1：Successful Shot + Authoritative Hitscan + Damage + Native Gun Noise。

DEV：`NativeGunAmmoGameTests`检查NBT、复制/容器/掉落实体序列化、部分装填、19tick单次提交及切槽取消。
`-I src/dev/native-gun-ammo-validation.init.gradle`只启用HUD实机截图，玩家进入世界后稳定显示1秒即保存
`run/screenshots/afl-v06-compact-hud-<current>-<reserve>-gui<scale>.png`；不自动操控输入或修改存档/背包。
这些DEV文件不进入发布JAR。旧文档中的“无限弹/尚无ammo或HUD”已被本节取代，视觉参数仍沿用冻结版本。



## 当前 Native AFL Gun Framework V0.5 — Blockbench Authoritative Rig

Service Pistol的枪物理尺寸、Ready双手、Fire双手、Reload双手与Display均由
`src/main/blockbench/service_pistol_v03_8_fire_slide_cleanup.bbmodel`制作并显式导出。
Runtime只接管当前物品、Gecko动画求值、animated locator→B_skin→真实玩家全臂/袖层。
Classic4×12×4/Slim3×12×4，canonical scale=1，共同Display=.41；不再去掉父缩放强设.246。
原Java BASE已等价迁入源FP Display；Reload侧开迁入fp_root关键帧，无Java曲线/.3除数。
旧gun-only .8效果烘焙到77个cube及机械平移，未重复历史.5缩枪；20 bones/183 keys。
Ready右主握/左非对称支撑；左手换弹与magazine平移配合，右手全程共同carrier主握。
Fire=.14s/Reload=1.30s，原无限弹药原型、网络/声音、CROSSBOW_HOLD、静态图标保持。
非FP继续逆物理尺寸兼容并跳过fp_root侧开；没有第一人称手部状态补丁。
DEV/release默认双手；测试失败只记日志，显式`/afl_nativegun_debug right|both`可切调试视图。
已删除旧ReloadGrip/LegacyHandMapping及失效DEV探针。制作流程与详细值见
`docs/dev/native-gun-hand-locator-authoring-standard.md`；验证进度见framework文档。
**用户Ready/Fire/Reload视觉验收=PENDING_USER，尚不宣称正式视觉合格。**

## 历史 Native V0.4.x（以下姿态与缩放描述已被V0.5取代）

### V0.4.9 Gun / Player Arm Scale 解耦候选

最新仅同步用户FP Display到service_pistol_in_hand.json：右translation=(-8.75,-.25,0)，
左=(-9.25,-.25,0)，两者scale=(.5,.5,.5)。第三人称和GUI资源未随source编辑改变。
原camera参数不改，固定arm style=.246不继承此display scale；本轮不重调枪/手姿势。

当前枪体由独立 `gun_model_root` 使用每武器 gunVisualScale=.8；双手 locator 位于
weapon_root 下另一个分支，不继承枪体缩放。NativePlayerArmRenderer 通用渲染真实
Classic/Slim全长手臂与袖层：保留动画locator位置/朝向、去除继承basis缩放，使用固定
框架style=.246，B_skin=1。shared camera/display参数不改，也不再决定玩家手的尺寸。
源bbmodel新增独立root和4个恒定scale关键帧；77个cube/UV/贴图、原132关键帧与音效不改。
旧.5物理迁移和non-FP2倍补偿保留，新增.8只对FP生效；CROSSBOW_HOLD/HUD/icon不改。
新标准模板 `src/main/blockbench/templates/afl_first_person_player_arm_rig.bbmodel`
包含source-only Classic/Slim左右臂和canonical轴。用户最新BB参考臂位置/右臂旋转已
等价折算到hand_anchor：右origin=(.1,8.42206877,1.46570503)、X=-60.5°；左origin=
(-.24988004,9.99753260,-.71193481)，左旋转保留；不加小补丁，不改枪/手比例。
本轮Right Only READY视觉待用户验收；左手/Reload视觉未验收。构建及数值结果见framework文档。

### 历史物理比例与右手READY候选（以下不是V0.4.9缩放规则）

V0.4.8.1 已被用户截图否决。当前在资产层仅将 gun 子树 77 个 cube 与相关 pivot
绕 (0,8,6) 缩为 .5；握把宽 4.8→2.4，枪内位移关键帧同步减半。手臂恢复
Classic 4×12×4 / Slim 3×12×4、B_skin=1，手部 locator 不作整树缩放。
最新单右手 READY 候选：手端=(0.1,7.75,9.2)、总 X=-65°，掌面倾角较前一版更平；
只更新右 source locator/reference 与 runtime anchor，未重新缩枪或调整 camera/B_skin。
本轮不改 Fire/Reload 文件，静态右姿态仍由各动作共用。左手仍隐藏，
只同步原 magazine 操作轨道的工作空间，不恢复完整方案。Fire/Reload 时长、旋转、
声音和玩法不变；共同 camera 参数冻结。76 个 box UV 固定为原取样的逐面 UV，贴图未改。
非第一人称 gun 分支逆变换补偿共享 geo 缩放，保留旧第三人称/掉落/展示框外观，
不改第三人称持枪 pose、图标、HUD。当前视觉未通过；构建与测试结果见 framework 文档。

### 历史 V0.4.8.1（已否决）

V0.4.8 的用户截图未通过：右手侧贴、前臂横伸、手细枪大。当前只重建单右手 READY。
共同取景改为 `T(0.51,-0.44,-0.70) × Ry(-4°) × Rx(-4°) × S(0.82)`，原 display=.30。
固定 B_skin 改为 `S(1.4) × T(-centreX,-10,0)/16`；完整原版 arm/sleeve，不按状态或武器特判。
枪有效 scale=.246，手=.3444；源 right_hand_anchor 经原 carrier 后手端=(0,8.8,12.9)，
total X=-32°，前臂向下后方。右参考臂和 runtime locator 同步，枪几何/贴图和动画关键帧不改。
左手继续 DEV 隐藏；其 locator/动画不改，但通用显示尺度在启用左手时也适用。
本轮不做 Fire/Reload 视觉调优；第三人称、声音、图标、HUD 和弹匣逻辑不动。
READY 视觉尚未通过，数值预览需显式应用 binding scale，原生 BB 视口不自动跟随 Java。

### 历史 V0.4.8（已被当前握持与比例候选替代）

V0.4.8 仅校准 Ready/Fire/Reload 共同第一人称 camera parent：右惯用手
`T(0.55,-0.47,-0.66) × Ry(-4°) × Rx(-4°) × S(0.92)`，左惯用手镜像 X/yaw。
原 display=0.30，枪与完整手臂共同有效 scale=0.276，B_skin=1 不变；没有单独放大
右手或重新校正接触 offset。取景改变投影和前臂露出量，不改变枪/手物理尺寸比。
既有 Reload 侧开曲线、动画、左手、第三人称与音效不改。当前候选待单右手 READY
用户验收，之后再验 Fire/Reload；不能把数值等价视为比例已通过。

第三人称已按用户同意的最小方案改用原版 CROSSBOW_HOLD，移除动态创建扩展
ArmPose 的默认路径，修正 ordinal=10 / switch length=10 崩溃原因。主辅手与惯用手
镜像由 Vanilla 处理，不再应用旧自定义角度，不新增模型 hook。构建与 960 次模型数值
调用通过；已进入世界，日志确认第三人称背面/正面持枪渲染未再越界，重复操作及视觉待确认。
V0.4.7 第一人称契约及 B1 单右手验收模式不变。

当前手部路线：canonical locator → 固定 Vanilla skin binding → 完整 arm/sleeve，
Ready/Fire/Reload 共用；-Y 为前臂方向，+Z 为掌面法线，+X 为横向。
binding scale=1、继承 display 0.30 与共同 presentation 0.92，不再运行薄掌面、短手或每枪方向补丁。
旧 anchor 保留为 motion carrier，参考臂变换折算到 canonical anchor；枪 geometry、
弹匣关键帧、左手换弹关键帧数值/时间和音效均冻结（取景仅按上述 V0.4.8 校准）。数值等价与用户视觉分别验收，
当前尚未视觉通过。艺术规范见 `docs/dev/native-gun-hand-locator-authoring-standard.md`。
下面 V0.4.1–V0.4.5.1 为历史过程，不代表当前手部默认路线。

新增 `apocalypse_firstlight:service_pistol`，GeckoLib 4.7.4 渲染 V0.3.8 正式资产。
主手左键半自动 Fire（3 tick 间隔），R Reload（26 tick 锁）；服务端确认后
同步动画和声音。无限弹药，无弹药状态、伤害或弹道；不接入下方 TaCZ 的
Noise / Tinnitus / Suppressor 规则。TaCZ 已改为仅开发运行时对比；旧Noise/Tinnitus转接已删除。
V0.4.1 增加静态 GUI 图标、由 animated hand anchors 驱动的玩家皮肤双臂，
以及仅对主手制式手枪生效的第三人称双臂上抬姿势。
V0.4.2 在 runtime 校准右手掌内主握与左前下支撑，调整前臂朝向和 camera-relative
位置；不修改原版手臂尺寸、源 rig 或导出的 display JSON。Reload 原动画实际在运行，
旧取景却让弹匣主要运动落到屏幕下方；现在于第一人称共同父变换增加平滑侧开、抬高和
拉远补偿，0–0.24s 进入、保持至 0.85s、1.18s 回到 Ready，完整动画仍为 1.30s。
双手继续读取 animated anchors；第三人称、图标、音效及锁定参数冻结。
用户在 V0.4.3 任务中认可并冻结当前枪械构图；旧弹匣出屏数值限制仍记录。
V0.4.3 只修玩家手臂：从 animated contact 提取无缩放的姿态，避免原版手臂继承
gun display 的 0.3 scale；保留 Classic/Slim 与左右 sleeve，校正前臂向下延伸。
初版单位缩放的手部过大且穿模，已被用户否决；经明确授权，修正版改为独立
Arm Visual Scale 0.55，并校准靠近手端/内侧的掌面接触，原版几何与源 anchors 不变。
0.55 版仍出现抱枪反馈，V0.4.4 已恢复 V0.4.2 略小的参考手臂映射，并为 Weapon/Arms
建立独立复制的矩阵作用域。审计未发现枪 scale/Z 改变或旧 push/pop 泄漏；保留已匹配的
V0.4.2 READY/FIRE 枪矩阵和当前 Reload 侧开，不继续手臂调参。旧参考映射仍有局部袖层/枪体
交叉，恢复不等于穿模已清除。用户已确认最终 READY 大小/距离/主辅手关系、FIRE、当前
Reload 侧开及连续操作回位可接受；V0.4.4 表现基线通过验收，ADS 未实现。
V0.4.5 厚短掌段未通过：有局部交叉，且当前侧开下被枪体遮挡。
V0.4.5.1 首版薄掌面贴在背缘，用户反馈像卡进枪里，数值通过但视觉失败。
侧面修正版仅在 Reload 使用玩家真实 skin 的薄握持表面：Classic 3.40×3.00×0.55 /
Slim 2.55×3.00×0.55，贴在朝镜头的握把 -X 侧面，epsilon=0.04 model units；取基础右手 UV，
不绘制右前臂或右袖层。右手固定相对握把，不增加时间驱动位移/旋转；左手继续操作弹匣。
Ready/Fire、枪侧开与动画音效当时冻结；侧面版也被用户否决，任务终止。V0.4.7 保留旧代码对照，但不默认调用它。
图形实机验收状态、资源路径、音效时间和限制见
`docs/dev/native-afl-gun-framework-v0.md`，不把实现状态当作实机验证。

## 2026-09-07 TaCZ 解耦 — 当前生产边界

TaCZ 不再是 AFL 强制依赖，`mods.toml` 不再声明 TaCZ；Gradle 仅保留
`runtimeOnly fg.deobf("curse.maven:timeless-and-classics-zero-1028108:8141310")` 供开发对比。
`-PaflWithoutTacz` 可省略此开发依赖。Native Gun Framework 是后续枪械功能唯一生产路线。
已删除 `TaczNoiseEvents`、`GunshotNoiseResolver`、枪械ID/类别fallback、附件silence读取及旧专用审计器。
TaCZ 开火不再产生 AFL 枪声 Noise / Tinnitus；但任何模组走标准 Forge Detonate 的爆炸
仍按通用爆炸入口处理，不屏蔽或重复监听第三方弹丸。

保留 NoiseSystem、感染者听觉、Explosion Noise、Explosion Tinnitus、共享Episode、Overlay/音频、
GunshotExposureTracker/Profile/Accumulator 的通用参数与冷却。未来 successful shot 的接入位置：
`NoiseSystem.emit(NoiseEvent, ServerLevel)`（类型 `GUNSHOT`）及
`GunshotExposureTracker.onGunshot(level, shooter, sourcePosition, acousticRadius, false)`。
后者仅发耳鸣impulse、不代发Noise；布尔参数是保留的通用策略输入，不再读取外部附件。
当前 Native Service Pistol 尚未调用这两个枪声入口；本轮未实现枪声pipeline或Native消音器。
Service Pistol 视觉/动画/声音、9mm资产、HUD及弹药逻辑未改。

删除专用TaCZ开发测试；保留爆炸、耳鸣核心测试，并用显式数值输入测试内部Noise听觉入口。
旧TaCZ测试通过记录仅属历史，不能视为当前集成仍存在或本轮运行验证。
