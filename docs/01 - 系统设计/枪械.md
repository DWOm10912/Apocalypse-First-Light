# 枪械

## Native AFL Gun Framework V0.4.4（动画/音效原型与第一人称表现层）

新增 `apocalypse_firstlight:service_pistol`，GeckoLib 4.7.4 渲染 V0.3.8 正式资产。
主手左键半自动 Fire（3 tick 间隔），R Reload（26 tick 锁）；服务端确认后
同步动画和声音。无限弹药，无弹药状态、伤害或弹道；不接入下方 TaCZ 的
Noise / Tinnitus / Suppressor 规则。TaCZ 原有逻辑与依赖不变。
V0.4.1 增加静态 GUI 图标、由 animated hand anchors 驱动的玩家皮肤双臂，
以及仅对主手制式手枪生效的第三人称双臂上抬姿势。
V0.4.2 在 runtime 校准右手掌内主握与左前下支撑，调整前臂朝向和 camera-relative
位置；不修改原版手臂尺寸、源 rig 或导出的 display JSON。Reload 原动画实际在运行，
旧取景却让弹匣主要运动落到屏幕下方；现在于第一人称共同父变换增加平滑侧开、抬高和
拉远补偿，0–0.24s 进入、保持至 0.85s、1.18s 回到 Ready，完整动画仍为 1.30s。
双手继续读取 animated anchors；第三人称、图标、音效及锁定参数冻结。
用户在 V0.4.3 任务中认可并冻结当前枪械构图；旧弹匣出屏数值限制仍记录。
V0.4.3 只修玩家手臂：从 animated contact 提取无缩放的姿态，避免原版手臂继承
gun display 的 0.3 scale；保留 Classic/Slim 与左右 sleeve，校正前臂向下延伸。
初版单位缩放的手部过大且穿模，已被用户否决；经明确授权，修正版改为独立
Arm Visual Scale 0.55，并校准靠近手端/内侧的掌面接触，原版几何与源 anchors 不变。
0.55 版仍出现抱枪反馈，V0.4.4 已恢复 V0.4.2 略小的参考手臂映射，并为 Weapon/Arms
建立独立复制的矩阵作用域。审计未发现枪 scale/Z 改变或旧 push/pop 泄漏；保留已匹配的
V0.4.2 READY/FIRE 枪矩阵和当前 Reload 侧开，不继续手臂调参。旧参考映射仍有局部袖层/枪体
交叉，恢复不等于穿模已清除；最终观感待验收，ADS 未实现。
图形实机验收状态、资源路径、音效时间和限制见
`docs/dev/native-afl-gun-framework-v0.md`，不把实现状态当作实机验证。

## Noise System V2：枪声显式半径

本表是 AFL 当前现实枪械裸枪 `GunId → gunshot noise radius` 的正式记录。半径单位为方块；代码 source-of-truth 为 `GunshotNoiseResolver.REFERENCE_RADII`。

| TaCZ GunId | 枪械 | 裸枪半径 |
| --- | --- | ---: |
| `tacz:m1911` | M1911 | 64 |
| `tacz:b93r` | B93R | 64 |
| `tacz:cz75` | CZ75 | 64 |
| `tacz:glock_17` | Glock 17 | 64 |
| `tacz:m9a4` | M9A4 | 64 |
| `tacz:p320` | P320 | 64 |
| `tacz:ump45` | UMP45 | 72 |
| `tacz:hk_mk23` | HK Mk23 | 72 |
| `tacz:hk_mp5a5` | HK MP5A5 | 72 |
| `tacz:uzi` | Uzi | 72 |
| `tacz:vector45` | Vector .45 | 72 |
| `tacz:p90` | P90 | 80 |
| `tacz:rhino357` | Rhino .357 | 80 |
| `tacz:m320` | M320 | 80 |
| `tacz:deagle` | Desert Eagle | 88 |
| `tacz:ak47` | AK-47 / AKM | 96 |
| `tacz:aug` | AUG | 96 |
| `tacz:g36k` | G36K | 96 |
| `tacz:hk416d` | HK416D | 96 |
| `tacz:m16a1` | M16A1 | 96 |
| `tacz:m16a4` | M16A4 | 96 |
| `tacz:m4a1` | M4A1 | 96 |
| `tacz:qbz_191` | QBZ-191 | 96 |
| `tacz:scar_l` | SCAR-L | 96 |
| `tacz:type_81` | Type 81 | 96 |
| `tacz:taurus500` | Taurus Raging Hunter .500 | 96 |
| `tacz:rpg7` | RPG-7 发射声 | 96 |
| `tacz:aa12` | AA-12 | 104 |
| `tacz:m1014` | M1014 | 104 |
| `tacz:m870` | M870 | 104 |
| `tacz:spas_12` | SPAS-12 | 104 |
| `tacz:sks_tactical` | SKS Tactical | 104 |
| `tacz:kar98` | Kar98 | 112 |
| `tacz:fn_evolys` | FN EVOLYS | 112 |
| `tacz:fn_fal` | FN FAL | 112 |
| `tacz:hk_g3` | HK G3 | 112 |
| `tacz:m249` | M249 | 112 |
| `tacz:m700` | M700 | 112 |
| `tacz:mk14` | Mk14 | 112 |
| `tacz:rpk` | RPK | 112 |
| `tacz:scar_h` | SCAR-H | 112 |
| `tacz:springfield1873` | Springfield 1873 | 112 |
| `tacz:ai_awp` | AWM / AWP | 128 |
| `tacz:m95` | M95 | 160 |
| `tacz:m107` | M107 | 160 |

显式表合计 **45** 把。下列 **9** 把本轮明确不写入显式表，继续按当前类别 fallback 或通用 `80` 格 fallback 解析：

| 未显式覆盖的 GunId |
| --- |
| `tacz:db_long` |
| `tacz:db_short` |
| `tacz:lonetrail` |
| `tacz:spr15hb` |
| `tacz:timeless50` |
| `tacz:deagle_golden` |
| `tacz:qbz_95` |
| `tacz:taurus943` |
| `tacz:minigun` |

显式半径和 fallback 都只产生基础半径。已安装 MUZZLE 的 TaCZ `silence` 值是 `Pair<Modifier, Boolean>`：`Modifier.addend` 提供 `distance_addend`，布尔值提供 `use_silence_sound`。只有该结构化布尔值为 `true` 才是 AFL true suppressor；不依赖名称或 ID 白名单。感染者最终半径为 `max(8, min(baseRadius + distance_addend, max(8, round(baseRadius × 0.10))))`；TaCZ 更强结果不会被抬高。非 Suppressor MUZZLE 只使用 `max(8, baseRadius + distance_addend)`。AFL 不复制或修改 TaCZ 枪械、附件或资源数据。

## Gunshot Tinnitus V1

玩家枪声耳鸣使用上表经 MUZZLE `silence.distance_addend` 修正、但未应用感染者 90% cap 的独立声学输入 `R`；没有第二份枪械参数表。True suppressor 在 listener 查询前直接令整次枪声 exposure contribution 为 `0`，同时覆盖射手和附近玩家；非 Suppressor 枪口不免疫。只检测同一维度声源周围 16 格内的玩家，射手按距离 `0`，其他玩家按眼睛位置到射手世界位置计算。Creative 包含，Spectator、死亡和 removed 玩家排除。

```text
loudness = clamp((R - 64) / 96, 0, 1)
distanceFactor = clamp(1 - distance / 16, 0, 1)^2
shotExposure = clamp(loudness * distanceFactor * 0.55 * caliberBoost, 0, 0.75)
caliberBoost = 1.25 when R >= 128, then additionally * 1.35 when R >= 160
```

| 代表枪械 | R | 射手单发 exposure | V1 行为 |
| --- | ---: | ---: | --- |
| M1911 | 64 | 0.000 | 单发及连续射击均不增加 exposure |
| UMP45 | 72 | 0.046 | 近距离需要大量连续射击 |
| AKM | 96 | 0.183 | 单发不触发；近距离短 burst 可跨过轻度阈值 |
| SPAS-12 | 104 | 0.229 | 近距离数发容易累积 |
| Kar98 | 112 | 0.275 | 单发不直接触发；连续两三发可触发 |
| AWM | 128 | 0.458 | 射手单发触发轻度；连续两发可加强 |
| M107 | 160 | 0.750（单发上限） | 射手单发明显触发；约 3 格玩家也可单发触发 |

Exposure 每 tick 衰减 `0.008`，阈值为 `0.35 / 0.60 / 0.90`；触发后保留 `45%`。Packet 至少间隔 6 ticks，且后续只在 severity 增加至少 `0.15` 或距上次发送满 20 ticks、需要延长 Episode 时发送。枪声和爆炸共用一个客户端 Episode：首个有效 impulse 播放一次 one-shot；后续冲击仅 strengthen / extend 同一 Envelope 和 Overlay，不停止、重建或从头播放声音，也不重置 Overlay 帧。时间轴结束后 re-arm；时长仍为 `round(40 + 120 × severity)` ticks，上限 200 ticks，Explosion Tinnitus 数值未改。

RPG-7、M320 的发射声和弹头爆炸声仍是位置不同、类型不同的两个 Noise Event；只有弹头实际进入标准 Explosion 流程时才产生动态 `EXPLOSION` 噪声和独立计算的 Explosion Tinnitus。Suppressor 只清零 Gunshot Tinnitus，不抑制 Explosion Tinnitus；裸枪半径、枪械数值和 TaCZ 资源均未改变。
