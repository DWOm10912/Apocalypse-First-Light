# 空仓换弹源动画契约

状态：空仓动作已接入运行时；左手接触路径修正后需要重新预览和实机验收，旧预览通过不代表这次修正通过。

源文件：`src/main/blockbench/service_pistol_v03_8_fire_slide_cleanup.bbmodel`，唯一目标为 `animation.service_pistol.reload_empty`。普通换弹、其他 clip、静态骨骼、几何与 Display 保持不变。

| 源动画节点 | 时间 / 值 |
|---|---:|
| 总时长 | 1.65 秒 |
| 原换弹段 | 0–0.95 秒沿用原轨道，仅空仓左手掌心增加同侧横向约束 |
| 抽匣 / 插匣 | 约 0.40 / 0.95 秒 |
| 插匣确认保持 | 0.95–1.12 秒 |
| 左手斜移到后部防滑纹 | 1.12–1.27 秒 |
| 左手接触、轻带 | 1.27–1.38 秒 |
| 声音起点（未改） | 1.25 秒 |
| 追加后拉 | 1.25–1.33 秒 |
| 后锁 / 最大后移 | 1.28 / 1.48 模型单位 |
| 释放、复进 | 1.43–1.46 秒 |
| 回支撑、回 Ready | 1.65 秒结束 |

左手通过 motion 骨骼求解朝内掌面边缘与 slide 局部坐标 `[-4.164, 9.9, 8.3]` 的接触；以真实同侧后部防滑纹为目标，不按相机最高角点选择接触边，不改 anchor 静态基线。1.12–1.27 秒直接插值到接触位置，不绕枪顶；1.27–1.38 秒随 slide 局部 Z 轴后移；1.38 秒开始释放，1.45–1.65 秒回支撑。枪体、右手、滑套机械轨道、声音均保持原样，因此滑套仍在 1.43–1.46 秒复进。slide、前后瞄具和 sight_anchor 同步后移；ejection_anchor 已是 slide 子骨骼，不重复添加位移。

局部空间检查：枪械纵向中心面为模型 X = -2.98；掌心中心全程保持 X ≤ -3.13。原有抽插匣段仅修正越界帧的横向位置，Y/Z 与旋转保留。接近段长度约 11.368 模型单位，直线距离约 11.359，路径比约 1.00082；接触段位移为 `[0, 0, 0.179296875]`，仅沿滑套前后轴。不再以屏幕中线作为通过条件。

验证边界：同侧全程检查针对掌心中心，不能替代整块手臂和袖层的碰撞验证。真实玩家臂的边缘重叠、接触观感及回位仍需目视确认。检查工具为 `tools/polish-empty-reload-left.mjs`。

Blockbench 声音关键帧引用 `E:/Download/slide_action.ogg`，不嵌入、不剪辑音频。文件为 48 kHz 双声道、0.480 秒；解码能量峰集中在约 0.06 和 0.20 秒，对应动画约 1.31 和 1.45 秒。声音与接触的最终吻合须预览确认，波形检查不等于试听通过。

运行时空仓计时 33 ticks，装弹仍在 tick 19，声音在 tick 25；动作开始时锁定空仓类型，完整动作结束前保持开火锁。普通换弹仍为 26 ticks。

`tools/export-native-gun.mjs --write --animations-only` 只写动画资源，不写 geo 或 Display。导出器校验并跳过该 source-only 声音预览轨道，由服务端在节点播放一次 `apocalypse_firstlight:service_pistol_slide_action`，避免双重播放。原始音频位于 `src/main/resources/assets/apocalypse_firstlight/sounds/weapons/service_pistol/slide_action.ogg`；运行时动画位于同一 assets 根目录的 `animations/service_pistol.animation.json`。

不实现膛室 NBT、17+1 或额外按键。取消沿用现有规则：装弹前不获得弹药，装弹后不退还或复制。
